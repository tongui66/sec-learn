// PHP 学习站 v2 · 第 21-24 章：实战与交付
var CHAPTERS_F = [
{
id:21, title:"实战一：单文件审计（15 行藏 4 个洞）", stage:"实战与交付",
intro:"这是全课程的第一个实战。<b>下面这段代码「看起来很正常」，但藏着四个经典漏洞。请你先自己找，再看答案。</b>",
sections:[
{h:"21.1 审计方法回顾", ol:["<b>标输入点</b>：把 $_GET / $_POST 出现的地方圈出来。",
"<b>标危险落点</b>：把 SQL 拼接、命令拼接、include、echo 圈出来。",
"<b>连线</b>：从输入点走到落点，中间有没有过滤。",
"<b>定级</b>：按危害排序。"]},
{h:"21.2 待审计的代码（先自己看）", code:[
`<?php
// ===== 用户资料查看页 user_info.php =====
$id  = $_GET['id'] ?? '1';
$fmt = $_GET['fmt'] ?? 'html';

// ① 查询用户
$sql = "SELECT name, role FROM user WHERE id = " . $id;
$row = mysqli_fetch_assoc(mysqli_query($conn, $sql));

// ② 导出为文本
if ($fmt === 'txt') {
    $cmd = 'echo "' . $row['name'] . '" > /tmp/export.txt';
    system($cmd);
    echo '已导出';
}

// ③ 模板渲染
include 'templates/' . $fmt . '.php';

// ④ 输出用户名
echo '用户名：' . $row['name'] . '<br>';`],
tip:"<b>先别看下面。</b>拿纸把四个漏洞的位置和类型写下来，再往下对答案——<b>自己找一遍，比直接看答案有用十倍。</b>"},
{h:"21.3 答案与逐条分析", tbl:[
["#","漏洞","位置","成因"],
["①","SQL 注入","`$sql = \"... WHERE id = \" . $id`","$id 来自 $_GET 且直接拼接"],
["②","命令执行（RCE）","`system('echo \"' . $row['name'] . '\" > ...')`","库里的 name 拼进系统命令"],
["③","文件包含（LFI）","`include 'templates/' . $fmt . '.php'`","$fmt 来自 $_GET，可传 ../ 跳出"],
["④","XSS","`echo '用户名：' . $row['name']`","输出未编码，且 name 可能是用户提交的"]],
note:"<b>注意第 ② 个漏洞的类型：它不是「用户输入直接进命令」，而是「数据库里的值进了命令」。</b>这就是<b>二次注入</b>的思路——攻击者之前在注册时把 <code>\"; rm -rf / #</code> 当成名字存进去，等管理员点开这一页时才触发。<b>「库里的数据不可信」这句话，在这里终于变成了具体的漏洞。</b>"},
{h:"21.4 修复对照", code:[
`<?php
// ① SQL 注入 -> 预处理
$stmt = $pdo->prepare('SELECT name, role FROM user WHERE id = ?');
$stmt->execute([$_GET['id'] ?? 1]);
$row = $stmt->fetch();
if (!$row) { die('用户不存在'); }

// ② 命令执行 -> 用 PHP 函数代替系统命令
$fmt = $_GET['fmt'] ?? 'html';
if ($fmt === 'txt') {
    file_put_contents('/tmp/export.txt', $row['name']);   // 不再调 shell
}

// ③ 文件包含 -> 白名单映射
$tpl = ['html' => 'templates/html.php', 'txt' => 'templates/txt.php'];
include ($tpl[$fmt] ?? $tpl['html']);

// ④ XSS -> 输出编码
function e($s) { return htmlspecialchars((string)$s, ENT_QUOTES, 'UTF-8'); }
echo '用户名：' . e($row['name']) . '<br>';`],
tip:"<b>注意修复的「类型」：</b>① 用参数化；② 用不同的函数（不是转义）；③ 用白名单；④ 用输出编码。<b>四种漏洞对应四种不同层面的修复</b>——这也是为什么「加个过滤函数」永远解决不了所有问题。"}
],
secview:"单文件审计的要点：<b>15 行代码能藏 4 个洞，而且每一个的成因都不同</b>（值拼接、数据进命令、路径可控、输出未编码）。真实项目就是这些模式的排列组合——你在这一章练的<strong>是「看到模式就条件反射」</strong>。"
},
{
id:22, title:"实战二：多文件小系统（从入口追到漏洞）", stage:"实战与交付",
intro:"单文件是练手，真实项目是多文件。<b>这一章演示「从入口文件出发，追到具体漏洞文件」的完整过程。</b>",
sections:[
{h:"22.1 项目结构（一个极简的文章管理系统）", code:[
`article-system/
├── index.php            <- 入口：所有请求都经过它
├── config.php           <- 数据库配置
├── lib/
│   ├── db.php           <- 数据库连接
│   └── helper.php       <- 公共函数（含输出函数）
└── module/
    ├── list.php         <- 文章列表
    ├── view.php         <- 查看文章
    ├── save.php         <- 保存文章（后台）
    └── upload.php       <- 上传封面`]},
{h:"22.2 入口文件：请求怎么分发", code:[
`<?php
// index.php
require 'config.php';
require 'lib/db.php';
require 'lib/helper.php';

session_start();

$act = $_GET['act'] ?? 'list';            // <- 输入点 1
$file = 'module/' . $act . '.php';

if (!file_exists($file)) {
    die('页面不存在');
}

include $file;                            // <- 危险落点 1（LFI）

// 审计结论：
//   这段代码看起来做了「file_exists 检查」，但**没有任何白名单或路径过滤**，
//   传入 ?act=../../../../etc/passwd%00 之类在老版本可绕过；
//   即使不能穿越，也能包含任意目录下名为 xxx.php 的文件。
//   -> 判定：文件包含漏洞（至少是任意文件包含）`],
note:"<b>注意这一段的审计价值：<code>file_exists()</code> 检查会给人「有防护」的错觉。</b>但它只是判断文件存在，不限制路径范围。<b>审计时看到「检查了但没限制范围」，一律按漏洞处理。</b>"},
{h:"22.3 公共文件：输出函数有没有问题", code:[
`<?php
// lib/helper.php

// 看起来是个「安全」的输出函数
function e($s) {
    return htmlspecialchars($s, ENT_QUOTES, 'UTF-8');
}

// 但下面这个函数忘了一件事
function show_avatar($url) {
    echo '<img src="' . $url . '">';      // <- 没有调用 e()！
}

// 审计要点：
//   同一个项目里，有的地方用了 e()，有的地方忘了用 —— 这是最常见的真实情况。
//   审计动作：搜所有 echo / 输出语句，逐个看有没有经过编码。
//   而不是「看到项目里有 e() 函数就认为安全」。`]},
{h:"22.4 后台功能：越权与上传", code:[
`<?php
// module/save.php —— 保存文章（应该只有管理员能用）
require '../lib/db.php';

// ❌ 只检查了登录，没检查角色
if (!isset($_SESSION['user'])) { die('请先登录'); }

$title   = $_POST['title'];
$content = $_POST['content'];

// ❌ 又是拼接 SQL
$sql = "INSERT INTO article (title, content) VALUES ('$title', '$content')";
mysqli_query($conn, $sql);

// module/upload.php —— 上传封面
$name = $_FILES['cover']['name'];
move_uploaded_file($_FILES['cover']['tmp_name'], 'uploads/' . $name);
// 三宗罪：文件名可控 + 无扩展名校验 + 上传目录在 Web 根下可解析

// 审计结论汇总：
//   save.php  : 垂直越权（普通用户可发文章）+ SQL 注入
//   upload.php: 任意文件上传（可直接上传 .php getshell）
//   index.php : 文件包含
//   helper.php: show_avatar 未编码 -> 存储型 XSS`]},
{h:"22.5 多文件审计的通用方法", ol:["<b>先画地图</b>：入口 → 路由 → 各功能文件 → 公共库。这一步花 10 分钟，省下后面几小时。",
"<b>看公共库</b>：db.php（怎么连库、有没有统一处理 SQL）、helper.php（输出函数、过滤函数）。<b>公共库决定了全站的「基线安全」。</b>",
"<b>逐个功能文件审</b>：按四步法走，每个文件记录发现。",
"<b>做交叉检查</b>：某个文件里发现的写法（比如拼接 SQL），去别处搜同款，通常能找出十几个同类漏洞。",
"<b>汇总定级</b>：按危害排序，RCE 和上传类优先。"]},
{h:"22.6 这套代码的漏洞清单（参考答案）", tbl:[
["文件","漏洞","成因"],
["index.php","任意文件包含","act 参数可控 + 无白名单"],
["module/save.php","垂直越权","只校验登录不校验角色"],
["module/save.php","SQL 注入","title/content 拼接进 INSERT"],
["module/upload.php","任意文件上传","文件名可控 + 无校验 + 目录可解析"],
["lib/helper.php","存储型 XSS","show_avatar 输出未编码"],
["module/view.php","（需自己看）","文章内容输出是否编码？权限是否校验？"]],
tip:"<b>最后一行是留给你的作业。</b>真实审计里没有人给你答案——<code>module/view.php</code> 我没写出来，就是让你自己补：<b>它输出文章内容时编码了吗？它读取文章时校验了「这篇是不是当前用户的」吗？</b>这两个问题的答案，决定了它是「正常」还是「两个新漏洞」。"}
],
secview:"多文件项目的审计顺序：<b>画地图 → 看公共库 → 逐个功能文件 → 交叉搜索同类 → 汇总定级</b>。真正省时间的不是「读得快」，而是<b>先建好地图，让每一行代码都知道自己在哪</b>。"
},
{
id:23, title:"实战三：组合利用链", stage:"实战与交付",
intro:"真实攻击很少「一个漏洞打到底」，而是<b>把几个中低危漏洞串成一条链</b>。这一章演示链条是怎么搭起来的——理解它，你才知道为什么「单独看每个洞都不严重」也是危险的。",
sections:[
{h:"23.1 一条典型的利用链", code:[
`目标系统：一个带上传功能的企业官网

漏洞清单（单独看都不致命）：
  A. 上传点只校验前端扩展名            （中危：上传绕过）
  B. 上传目录在 Web 根下且能解析 PHP   （高危配置）
  C. 存在 SSRF（图片远程抓取功能）      （中危）
  D. 内网有一台 Redis 未授权            （内网资产）

攻击链：
  ① 用 SSRF 探测内网，发现 Redis 可访问
  ② 发现 Redis 能写文件（配置 set dir / dbfilename）
  ③ 把 WebShell 内容写进 A 上传目录下的 xxx.php
  ④ 访问该文件 -> getshell

审计启示：
  · 「上传绕过」+「目录可解析」= 高危，不能分开看
  · SSRF 的危害取决于内网有什么（先摸内网再进行评估）
  · 定级时要写「组合危害」，而不是只写单项`],
note:"<b>为什么要把这个例子讲清楚？</b>因为很多人在 SRC 里提交漏洞时会写「上传绕过（中危）」——而审核方可能因为「上传目录不可解析」而不收。<b>理解链条，才能准确评估危害，也才能在报告里把影响写清楚。</b>"},
{h:"23.2 常见的高危组合", tbl:[
["组合","升级结果"],
["文件包含 + 任意文件上传","LFI 变 RCE（包含上传的图片马）"],
["文件包含 + 日志可读","LFI 变 RCE（包含日志里的 payload）"],
["SQL 注入 + FILE 权限","拖库升级为写文件 getshell"],
["SSRF + 内网服务","穿透边界，攻击内网（Redis、Docker API、K8s API）"],
["反序列化入口 + 框架版本存在 gadget","反序列化变 RCE"],
["任意文件读取 + 配置文件","拿到数据库/云厂商凭证，横向扩展"],
["XSS + 后台功能","打管理员 Cookie，接管后台"],
["越权 + 批量接口","一次遍历导出全站数据"]],
tip:"<b>审计时的实用习惯：找到一个「中危」漏洞后，不要立刻收手，先问一句——「它能和别的东西组合吗？」</b>这个问题经常能把一个中危变成高危，也常常是能不能拿奖励的关键。"},
{h:"23.3 从漏洞到利用：思路清单", ol:["<b>先看危害上限</b>：这个漏洞最坏能到什么程度？（读文件 / 写文件 / 执行代码？）",
"<b>找配套条件</b>：要达成上限，还差什么？（需要一个上传点？需要路径已知？需要权限？）",
"<b>在系统里找那个条件</b>：还差的那个东西，这个系统里有没有？",
"<b>验证链条</b>：一步一验证，不要跳步。",
"<b>评估整体危害</b>：按链条的最终效果定级，而不是按第一步定级。"]}
],
secview:"组合利用链的核心认知：<b>漏洞的危害不由它本身决定，而由「它能接到什么」决定。</b>审计时养成一个习惯——找到一个洞之后问「它还能接上什么」，这既能让你的报告更有价值，也能让你更准确地判断风险。"
},
{
id:24, title:"审计报告怎么写", stage:"实战与交付",
intro:"审计的成果最终要落到报告上。<b>一份好报告能让开发立刻改对代码；一份差报告只会被回一句「已修复」然后不了了之。</b>这一章给一份可直接套用的模板。",
sections:[
{h:"24.1 报告的基本结构", tbl:[
["部分","写什么","要点"],
["① 概要","漏洞名称、危害等级、影响范围","一两句话说清「这是什么、有多严重」"],
["② 位置","文件路径 + 行号 + 关键代码片段","越精确越好，开发才能找到"],
["③ 成因","为什么这是漏洞","引用代码逻辑，讲清数据流"],
["④ 复现","请求示例 / 操作步骤 / 结果截图","让别人能复现出来"],
["⑤ 影响","能造成什么后果","结合业务讲：能拿什么数据、能做什么操作"],
["⑥ 修复","具体代码改法 + 复测方法","给代码，不要给口号"],
["⑦ 备注","同类问题位置、参考链接","顺手指出别处还有几处"]],
note:"<b>第 ⑦ 项是加分项，也是专业度的体现。</b>「同一个拼接写法在 <code>admin/goods.php:88</code> 和 <code>api/order.php:42</code> 也出现了」——这一句能让开发一次改完所有地方，而不是修一个漏一个。"},
{h:"24.2 一个完整的报告示例", code:[
`【漏洞】后台文章保存接口存在 SQL 注入
【等级】高危（可拖取全库数据）

【位置】
  文件：/admin/module/save.php
  行号：第 12-14 行
  代码：
      $title = $_POST['title'];
      $sql   = "INSERT INTO article (title, content) VALUES ('$title', '$content')";
      mysqli_query($conn, $sql);

【成因】
  $title / $content 直接来自 POST 参数，未做任何处理就拼接进 SQL 语句，
  用户输入的引号和 SQL 关键字会被数据库当作语法解析。

【复现步骤】
  1. 登录普通账号（该接口未做角色校验，见附注）
  2. 提交文章，title 参数填：
         test'), (SELECT @@version) -- 
  3. 请求成功后查询 article 表，可见数据库版本被插入
  4. 进一步可用 UNION / 报错注入读取 user 表内容

【影响】
  1. 可读取整库数据（用户名、密码哈希、订单信息）
  2. 数据库账号权限若较大，可进一步写文件
  3. 结合该接口的角色校验缺失，普通用户即可触发

【修复建议】
  1. 改用预处理（PDO 示例）：
        $stmt = $pdo->prepare('INSERT INTO article (title, content) VALUES (?, ?)');
        $stmt->execute([$title, $content]);
  2. 该接口增加角色校验：
        if (($_SESSION['role'] ?? '') !== 'admin') { http_response_code(403); exit; }
  3. 数据库账号按最小权限配置（参考附注 2）

【复测方法】
  提交同样的 payload，接口应正常保存「test'), (SELECT @@version) -- 」这段文本，
  而不是把它当 SQL 执行。

【附注】
  1. 同类拼接问题还出现在：/admin/module/list.php:23、/api/search.php:31
  2. 当前数据库账号为 root，建议改为仅授权必要库的最小权限账号`],
tip:"<b>注意这份报告的三个细节：</b>① 复现步骤是可执行的，不是「随便注入一下」；② 修复建议给了可复制的代码；③ 附注列出了同类问题。<b>这三点决定了报告是被重视还是被忽略。</b>"},
{h:"24.3 写报告时的常见错误", tbl:[
["错误写法","问题","改成"],
["「存在 SQL 注入漏洞，建议过滤参数」","没位置、没成因、没代码","给文件行号 + 成因 + 预处理代码"],
["等级全写「高危」","无法排优先级，显得不专业","按危害金字塔如实定级"],
["复现步骤写「用 sqlmap 跑」","对方可能无法复现，也不利于修复验证","给最小可复现的手工请求"],
["修复建议写「加强校验」","开发不知道怎么改","给具体函数与代码片段"],
["只报一个点","修完还有十个同类","顺手列出所有同类位置"],
["夸大危害","失去可信度","危害按「已证实的上限」写"]]},
{h:"24.4 从审计到交付的完整闭环", ol:["<b>审计</b>：四步法找出漏洞（第 18 章）。",
"<b>验证</b>：本地跑起来确认可利用性（第 19 章）。",
"<b>定级</b>：按危害金字塔与组合影响评估（第 23 章）。",
"<b>写报告</b>：位置、成因、复现、影响、修复、复测。",
"<b>修复验证</b>：开发改完后，用同样的 payload 复测——<b>这一步很多人会漏，但它决定了这个漏洞是不是真的被修掉了</b>。",
"<b>沉淀</b>：把这次的写法与绕过整理进自己的检查清单，下次审计直接复用。"]},
{h:"24.5 最后一句话", p:["到这里，24 章走完了。你现在应该具备三件事：<b>① 读得懂 PHP 代码；② 知道危险写法和安全写法的区别；③ 有一套可执行的审计流程。</b>",
"接下来最有效的提升方式只有一条：<b>找一个真实开源项目，从头到尾审计一遍，然后写一份报告（哪怕没有提交对象）。</b>第一个项目会很难，第二个就快了，第五个你会开始「看几眼就知道该往哪看」。"]}
],
secview:"报告的价值在于「让问题被修掉」。<b>给位置、给成因、给代码、给复测方法、列同类问题</b>——这五样齐了，才算一份专业的审计交付。而修复后的复测，是把闭环真正闭上的一步。"
}
];
