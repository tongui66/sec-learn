// 防火墙学习站 v2 · 实验手册
var LABS = [
{
 title:"实验 1 · Windows：用 wf.msc 放行一个端口（零成本入门）",
 meta:"难度 ★☆☆　用时 10 分钟　环境：你自己的 Windows",
 goal:"亲手走一遍「服务监听 → 防火墙规则 → 外部可达」的完整链路，建立对防火墙作用的直觉。",
 env:"任意一台 Windows 电脑，管理员权限。用手机热点或另一台设备做外部测试（没有也行，用 127.0.0.1 与局域网 IP 的差异来观察）。",
 steps:["在本机临时起一个 Web 服务：`python -m http.server 8000`（有 Python 的话），或者用任何已在监听端口的程序。",
"用 `netstat -ano | findstr :8000` 确认它确实在监听。",
"Win+R 打开 `wf.msc`，新建入站规则：端口 → TCP → 8000 → 允许连接 → 三个配置文件都勾上 → 命名。",
"从另一台设备（手机浏览器）访问 `http://本机局域网IP:8000`，确认能打开。",
"回到 wf.msc，把这条规则右键禁用，再刷新手机页面——观察现象变化。"],
 code:"# 看监听\nnetstat -ano | findstr :8000\n\n# 看当前网络类型（决定哪个配置文件生效）\nGet-NetConnectionProfile\n\n# 查所有 8000 相关规则\nGet-NetFirewallRule -DisplayName \"*8000*\" | Get-NetFirewallPortFilter",
 verify:["规则启用时：外部设备能打开页面","规则禁用时：外部设备连接超时","本机用 127.0.0.1 访问：无论规则启用与否都成功（因为回环不过防火墙）"],
 undo:["在 wf.msc 里删除该规则，或 `Remove-NetFirewallRule -DisplayName \「规则名\"`"],
 why:"这个实验最重要的是最后一条验证：<b>本机永远能连，因为回环流量不经过防火墙</b>。理解这一点，你以后就不会再把「本机能连」当成「配置正确」的证据。"
},
{
 title:"实验 2 · Windows：用 PowerShell 写一条白名单规则",
 meta:"难度 ★☆☆　用时 15 分钟　环境：Windows + PowerShell（管理员）",
 goal:"掌握用命令行管理规则的完整流程：建规则 → 查规则 → 改规则 → 删规则。",
 env:"Windows 10 / 11，管理员 PowerShell。",
 steps:["新建一条只允许 192.168.1.0/24 访问 3389 的入站规则。",
"用 Get-NetFirewallRule 配合 Get-NetFirewallPortFilter / Get-NetFirewallAddressFilter 把它拆开看清楚。",
"先禁用再启用这条规则，观察状态字段的变化。",
"查看所有「阻止类」规则——这是排查「放行了却不通」时最关键的一步。",
"最后删除实验规则，恢复原状。"],
 code:"# 建规则：只允许内网网段访问 RDP\nNew-NetFirewallRule -DisplayName \「实验-内网RDP\" `\n  -Direction Inbound -Protocol TCP -LocalPort 3389 `\n  -RemoteAddress 192.168.1.0/24 -Action Allow -Profile Any\n\n# 查规则的三件套：主表 + 端口过滤 + 地址过滤\nGet-NetFirewallRule -DisplayName \」实验-内网RDP\"\nGet-NetFirewallRule -DisplayName \「实验-内网RDP\" | Get-NetFirewallPortFilter\nGet-NetFirewallRule -DisplayName \」实验-内网RDP\" | Get-NetFirewallAddressFilter\n\n# 禁用 / 启用\nDisable-NetFirewallRule -DisplayName \「实验-内网RDP\"\nEnable-NetFirewallRule  -DisplayName \」实验-内网RDP\"\n\n# 查所有阻止类规则（排查隐藏拦截）\nGet-NetFirewallRule -Action Block -Enabled True | Select-Object DisplayName, Direction, Profile\n\n# 删除\nRemove-NetFirewallRule -DisplayName \「实验-内网RDP\」",
 verify:["Get-NetFirewallRule 能查到该规则，Enabled 字段为 True","PortFilter 显示 LocalPort = 3389","AddressFilter 显示 RemoteAddress = 192.168.1.0/24"],
 undo:["`Remove-NetFirewallRule -DisplayName \「实验-内网RDP\"`"],
 why:"一条 Windows 防火墙规则并不是一个单一对象，而是主记录 + 端口过滤 + 地址过滤 + 应用过滤的组合。<b>看懂这个「拆分结构」，你才能理解为什么有些规则看起来存在却不生效。</b>"
},
{
 title:"实验 3 · Linux：配一台「不会把自己锁死」的最小防火墙",
 meta:"难度 ★★☆　用时 30 分钟　环境：Ubuntu 虚拟机（务必先打快照）",
 goal:"从零配出一台有状态、默认拒绝、但工作正常的防火墙，并完整理解每一步的作用。",
 env:"Ubuntu 虚拟机。可以在 VMware/VirtualBox 里用「仅主机」或 NAT 网络，避免影响宿主机。先拍快照。",
 steps:["先看当前状态：`sudo iptables -L -n` （新系统通常是空的，默认策略 ACCEPT）。",
"按正确顺序加入三条保命规则：lo、ESTABLISHED/RELATED、SSH。",
"验证这三条规则已经存在并且顺序正确（`--line-numbers`）。",
"此时再设默认策略为 DROP——注意：如果你是通过 SSH 连的，这一步之后连接应该仍然正常。",
"追加 80/443 的放行，并验证计数器的变化。",
"重启网络（`sudo systemctl restart networking`）观察规则是否还在——预期是<b>不在了</b>，这正好说明持久化的必要性。"],
 code:"# ① 看现状\nsudo iptables -L -n -v\n\n# ② 保命三连（顺序不可颠倒）\nsudo iptables -A INPUT -i lo -j ACCEPT\nsudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT\n\n# ③ 确认已存在\nsudo iptables -L INPUT -n -v --line-numbers\n\n# ④ 现在才改默认策略\nsudo iptables -P INPUT DROP\nsudo iptables -P FORWARD DROP\n\n# ⑤ 追加业务端口\nsudo iptables -A INPUT -p tcp -m multiport --dports 80,443 -j ACCEPT\n\n# ⑥ 验证：另开一个终端测试 SSH 仍然可用，再看计数\nsudo iptables -L INPUT -n -v",
 verify:["`sudo iptables -L INPUT -n` 能看到 lo、ctstate、22 三条规则排在默认策略之前","SSH 连接在所有操作完成后依然可用","`ping 本机外网地址` 仍然成功（因为出站是放行的，且回包命中 ESTABLISHED）","重启网络后规则消失（验证了「不持久化就会丢」）"],
 undo:["`sudo iptables -P INPUT ACCEPT && sudo iptables -F` 即可恢复干净状态（实验环境专属操作）"],
 why:"这一步「先放行、后拒绝」的顺序，是所有防火墙配置的通用法则。<b>它不只适用于 iptables，云安全组、firewalld、Windows 防火墙都一样</b>：先把必要的通道打开，再收紧默认策略。"
},
{
 title:"实验 4 · 观察 conntrack：看见「连接」这个抽象概念",
 meta:"难度 ★★☆　用时 20 分钟　环境：Linux 虚拟机",
 goal:"把状态检测这个抽象概念变成你能看见的数据：亲眼看一条连接从 NEW 变成 ESTABLISHED。",
 env:"Ubuntu 虚拟机（conntrack 工具可能需要 `sudo apt install conntrack`）。",
 steps:["打开两个终端。终端 A 持续观察 conntrack 表。",
"终端 B 发起一个连接（如 `curl -I https://example.com` 或 `ssh 另一台机器`）。",
"在终端 A 里找到这条连接，读它各个字段：协议、剩余时间、状态、正向与反向五元组。",
"观察 [UNREPLIED] 标记从有到无的变化（收到回包后消失）。",
"再发起一次 `ping`（ICMP），对比它和 TCP 在表里长什么样。",
"最后看统计：`conntrack -S`，理解不同协议的条目数量级差异。"],
 code:"# 终端 A：持续观察（-n 不解析域名，便于肉眼对比）\nwatch -n 1 \"sudo conntrack -L -n | head -20\"\n\n# 或者直接看内核里的原始记录\nsudo cat /proc/net/nf_conntrack\n\n# 终端 B：制造几条连接\ntimeout 3 ping -c 3 8.8.8.8\ncurl -I https://example.com\n\n# 统计各类状态的条目数量\nsudo conntrack -S\n\n# 只看 443 相关的\nsudo conntrack -L -p tcp --dport 443\n\n# 看当前用量和上限\ncat /proc/sys/net/netfilter/nf_conntrack_count\ncat /proc/sys/net/netfilter/nf_conntrack_max",
 verify:["能看到 TCP 记录里有 NEW 和 ESTABLISHED 两种状态","ICMP 也会生成记录（因为 ping 也是一种「流」）","连接关闭/超时后，记录会自动消失（超时倒计时归零）"],
 undo:["无需回滚，本实验只读不写（ping 和 curl 不会修改防火墙规则）"],
 why:"「状态检测」在很多人的理解里只是一个名词。这个实验把它变成一张张能读的表：<b>你会亲眼看到防火墙是怎么「记住」一条连接的</b>，也会理解为什么一条长连接会在表里待很久。"
},
{
 title:"实验 5 · 用 recent 模块做 SSH 自动封禁",
 meta:"难度 ★★★　用时 30 分钟　环境：两台 Linux 虚拟机（或虚拟机 + 宿主机）",
 goal:"不依赖任何第三方软件，用 iptables 原生能力实现「失败次数过多就自动封禁」。",
 env:"两台机器：一台当靶机（配规则），一台当攻击机（反复尝试连接）。也可以在同一台机器上用两个终端模拟。",
 steps:["在靶机上按顺序建立四条规则：查黑名单 → 放行已建立 → 记录新连接 → 超阈值封禁。",
"注意顺序：查黑名单必须放在最前面，否则后面的 ACCEPT 会让它永远匹配不到。",
"在攻击机上反复发起 SSH 连接尝试（用错误密码或直接 telnet 22 端口）。",
"在靶机上观察 `/proc/net/xt_recent/SSH_BAN` 里的来源列表，看计数如何增长。",
"超过阈值后再从攻击机尝试连接，应该表现为超时（被 DROP）。",
"查看规则计数器，验证封禁规则确实被命中。"],
 code:"# ① 已封禁的直接丢弃（放最前面）\nsudo iptables -I INPUT 1 -p tcp --dport 22 -m recent --name SSH_BAN --rcheck --seconds 600 -j DROP\n\n# ② 放行已建立连接，避免把自己踢掉\nsudo iptables -A INPUT -p tcp --dport 22 -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\n\n# ③ 记录新连接来源\nsudo iptables -A INPUT -p tcp --dport 22 -m conntrack --ctstate NEW -m recent --name SSH_BAN --set -j ACCEPT\n\n# ④ 600 秒内超过 4 次就进黑名单\nsudo iptables -A INPUT -p tcp --dport 22 -m recent --name SSH_BAN --update --seconds 600 --hitcount 4 -j DROP\n\n# 观察名单\ncat /proc/net/xt_recent/SSH_BAN\n\n# 看计数器（哪条规则在起作用）\nsudo iptables -L INPUT -n -v --line-numbers",
 verify:["反复尝试连接后，名单文件里出现来源地址并且计数增长","超过阈值后新连接表现为超时（DROP 的特征）","你自己的正常 SSH 会话不会被中断（因为已建立连接被放行）"],
 undo:["`sudo iptables -D INPUT -p tcp --dport 22 -m recent --name SSH_BAN --rcheck --seconds 600 -j DROP` 等逐条删除，或直接 `sudo iptables -F`（实验环境）","清空名单：`echo clear > /proc/net/xt_recent/SSH_BAN`"],
 why:"这个实验把「检测 + 响应」这条闭环压缩到了四条规则里。<b>理解它之后，fail2ban 对你来说就不再是黑盒</b>——它做的事情就是读日志 + 调防火墙，原理完全一致。"
},
{
 title:"实验 6 · ipset：一条规则管理上万条黑名单",
 meta:"难度 ★★☆　用时 20 分钟　环境：Linux 虚拟机",
 goal:"理解「集合」这种匹配方式为什么比堆规则更高效，并对比两种写法的差异。",
 env:"Ubuntu 虚拟机，`sudo apt install ipset`。",
 steps:["创建 hash:ip 类型的集合，带 3600 秒超时。",
"批量往里加一些地址（可以脚本生成 1000 个测试地址）。",
"用一条 iptables 规则引用这个集合。",
"对比：如果不用 ipset，你需要 1000 条规则——用 `iptables -L` 观察两者在规则数量和匹配开销上的区别。",
"演示动态增删：往集合里加/删一个地址，观察防火墙行为立即变化（不需要 reload）。"],
 code:"# 创建集合（带超时，自动过期）\nsudo ipset create blacklist hash:ip timeout 3600\n\n# 批量添加（示例：生成 1000 个测试地址）\nfor i in $(seq 1 1000); do sudo ipset add blacklist 198.51.$((i/256)).$((i%256)) 2>/dev/null; done\n\n# 一条规则引用整个集合\nsudo iptables -I INPUT -m set --match-set blacklist src -j DROP\n\n# 查看集合规模与内容\nsudo ipset list blacklist | head -20\nsudo ipset list blacklist | wc -l\n\n# 动态增删（立即生效，无需 reload）\nsudo ipset add blacklist 203.0.113.66\nsudo ipset del blacklist 203.0.113.66\n\n# 对比：规则数量与匹配效率\nsudo iptables -L INPUT -n -v",
 verify:["`ipset list blacklist` 能看到集合条目","iptables 里只有一条规则，却能覆盖上千个地址","`ipset del` 之后，该地址立刻可以访问（无需重载规则）"],
 undo:["`sudo iptables -D INPUT -m set --match-set blacklist src -j DROP`","`sudo ipset destroy blacklist`"],
 why:"企业里的自动封禁系统几乎都长这样：<b>检测程序负责往集合里增删 IP，防火墙永远只有一条规则。</b>这个「策略稳定、数据变化」的架构思路，比规则怎么写重要得多。"
},
{
 title:"实验 7 · NAT 共享上网：把你的虚拟机变成网关",
 meta:"难度 ★★★　用时 40 分钟　环境：一台双网卡 Linux 虚拟机",
 goal:"让一台 Linux 充当网关，让内网的另一台机器通过它访问外网。做完这个实验，NAT 就不再是抽象概念。",
 env:"一台双网卡 Linux 虚拟机（如 eth0 接 NAT 网络、eth1 接仅主机网络），另加一台做内网客户端的虚拟机。",
 steps:["确认两块网卡的 IP 规划，记录内网网段和出口网卡名（这一步最容易搞错）。",
"开启 ip_forward，并确认 `/proc/sys/net/ipv4/ip_forward` 的值变成 1。",
"在 POSTROUTING 上配置 SNAT（固定出口用 SNAT）或 MASQUERADE（动态出口）。",
"必须放行 FORWARD 链的双向流量——这是最常被遗忘的一步。",
"把内网客户端的默认网关指向这台机器，然后在客户端上 ping 外网。",
"在网关上用 conntrack 观察 NAT 会话，看地址是怎么被改写的。"],
 code:"# ① 看清楚网卡和地址（先别急着配规则）\nip -br addr\nip route\n\n# ② 开启转发\nsudo sysctl -w net.ipv4.ip_forward=1\ncat /proc/sys/net/ipv4/ip_forward    # 应为 1\n\n# ③ 出站做源地址转换（假设 eth0 是内网侧 192.168.56.0/24，eth1 是出口）\nsudo iptables -t nat -A POSTROUTING -s 192.168.56.0/24 -o eth1 -j MASQUERADE\n\n# ④ 放行转发流量（双向，缺一不可）\nsudo iptables -A FORWARD -i eth0 -o eth1 -j ACCEPT\nsudo iptables -A FORWARD -i eth1 -o eth0 -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\n\n# ⑤ 客户端侧：把默认网关指向网关内网地址\n#    ip route add default via 192.168.56.1\n\n# ⑥ 网关上观察 NAT 会话\nsudo conntrack -L -n | grep MASQ\nsudo iptables -t nat -L POSTROUTING -n -v",
 verify:["客户端 `ping 8.8.8.8` 成功","客户端的 `curl ifconfig.me`（或访问任意网站）能返回内容","网关的 `iptables -t nat -L POSTROUTING -n -v` 计数器在增长","conntrack 里能看到带 NAT 标记的会话"],
 undo:["客户端恢复原网关设置","网关上 `sudo iptables -t nat -F POSTROUTING && sudo iptables -F FORWARD`","`sudo sysctl -w net.ipv4.ip_forward=0`"],
 warn:"如果你的虚拟机网络用的是「桥接」，请格外小心：错误的 NAT/转发配置可能影响所在局域网。练习时优先使用「NAT 网络」或「仅主机」模式。",
 why:"NAT 的三件事在这个实验里会同时出现：<b>转发开关、地址转换、FORWARD 放行</b>。任何一个漏掉都不通，而排查顺序也正好是这三步——这就是真实工作里的排查逻辑。"
},
{
 title:"实验 8 · DNAT 端口转发与 hairpin 陷阱",
 meta:"难度 ★★★　用时 40 分钟　环境：双网卡 Linux 网关 + 内网 Web 服务",
 goal:"把内网服务发布到网关的某个端口上，然后亲手踩一次 hairpin 的坑并修好它。",
 env:"承接实验 7 的环境：网关上再跑一个内网 Web 服务（或内网客户端上跑一个）。",
 steps:["在内网机器上起一个 Web 服务（`python3 -m http.server 80`）。",
"在网关的 PREROUTING 上配置 DNAT，把网关的 8080 映射到内网机器的 80。",
"<b>故意先不写 FORWARD 规则</b>，从外部访问，观察现象（超时/不通）——记住这个现象。",
"补上 FORWARD 放行（注意：写的是转换后的内网地址），再访问，这次应该通。",
"现在从<b>内网客户端</b>访问网关的公网 IP:8080——如果不通，说明遇到了 hairpin 问题。",
"补一条内网到内网目标的 SNAT，再测，这次应该通了。"],
 code:"# ① 内网机器起服务\npython3 -m http.server 80\n\n# ② 网关上做目的地址转换（公网 8080 → 内网 192.168.56.10:80）\nsudo iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to-destination 192.168.56.10:80\n\n# ③ 放行转发（注意写的是「转换后」的内网地址）\nsudo iptables -A FORWARD -p tcp -d 192.168.56.10 --dport 80 -j ACCEPT\nsudo iptables -A FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\n\n# ④ hairpin：让内网访问公网地址的流量也经过网关转换\nsudo iptables -t nat -A POSTROUTING -s 192.168.56.0/24 -d 192.168.56.10 -p tcp --dport 80 -j SNAT --to-source 192.168.56.1\n\n# ⑤ 验证\ncurl -I http://网关的对外IP:8080\nsudo conntrack -L -n | grep 192.168.56.10\nsudo iptables -t nat -L PREROUTING -n -v",
 verify:["外部访问网关 8080 能打开内网服务页面","在没写 FORWARD 规则时，访问不通（验证了「NAT 只改地址、放行归 filter 管」）","补上 hairpin 规则后，内网访问公网 IP 也能通","conntrack 里能看到 DNAT 会话记录"],
 undo:["`sudo iptables -t nat -F PREROUTING`","`sudo iptables -t nat -F POSTROUTING`","`sudo iptables -F FORWARD`"],
 why:"这个实验包含两个重要的认知：<b>第一，NAT 和放行是两件事</b>（nat 表改地址，filter 表管通不通）；<b>第二，hairpin 是拓扑问题</b>，靠加规则解决，而不是去怀疑服务。"
},
{
 title:"实验 9 · 抓包排障：亲手制造一个「连不上」并定位它",
 meta:"难度 ★★★　用时 30 分钟　环境：两台 Linux 虚拟机",
 goal:"用抓包把「猜测」变成「证据」，掌握最实用的排障方法。",
 env:"两台 Linux 虚拟机（A 客户端、B 服务端），能在两端同时抓包。",
 steps:["在 B 上起一个 Web 服务并确认监听。",
"在 B 上用 iptables 把 80 端口 DROP 掉（制造故障）。",
"在 A 上访问 B:80，同时在 A 和 B 两端抓包，观察包走到了哪里。",
"对比两端结果，得出结论：包到了 B、B 没有回应 → 问题在 B 的本机防火墙。",
"把 DROP 改成 REJECT，再访问一次，观察抓包里出现了 RST——客户端立刻收到「连接被拒绝」。",
"恢复规则，总结「超时」和「被拒绝」两种现象分别对应什么设备行为。"],
 code:"# A 客户端抓包\nsudo tcpdump -i any -nn host 192.168.56.10 and port 80\n\n# B 服务端抓包\nsudo tcpdump -i any -nn port 80\n\n# B 上制造故障：DROP（静默丢弃）\nsudo iptables -A INPUT -p tcp --dport 80 -j DROP\n\n# 改成 REJECT（明确拒绝），对比抓包结果\nsudo iptables -D INPUT -p tcp --dport 80 -j DROP\nsudo iptables -A INPUT -p tcp --dport 80 -j REJECT --reject-with tcp-reset\n\n# A 上测试\ncurl -v --max-time 5 http://192.168.56.10/",
 verify:["DROP 时：A 上看到反复重传的 SYN，B 上能看到 SYN 进来但没有 SYN-ACK，A 最终超时","REJECT 时：A 上能抓到一个返回的 RST，curl 立刻报 connection refused","能根据「两端抓包」明确判断问题出在哪一侧"],
 undo:["`sudo iptables -F INPUT`"],
 why:"「抓包 + 两端对比」是网络排障最有力的手段。它能把你的判断从「我觉得是防火墙的问题」变成<b>「服务端收到了 SYN 但没有回应，问题在服务端本机」</b>——结论明确，不需要猜。"
},
{
 title:"实验 10 · 审计练习：找出这份规则集的问题",
 meta:"难度 ★★★　用时 25 分钟　环境：Linux 虚拟机（只读分析，配好即测）",
 goal:"训练「看规则就能发现风险」的能力，这是运维和安全岗的核心技能之一。",
 env:"任意 Linux 虚拟机。把下面的问题规则抄进去，然后逐条审计。",
 steps:["把参考代码里的「问题规则集」抄进虚拟机（只在实验环境里抄，别在生产机器上试）。",
"对照第 19 章的审计清单，逐条检查：默认策略、顺序、过度开放、管理端口来源、出站、日志。",
"用 `iptables -L -n -v --line-numbers` 找到所有「没有限定来源的入站放行」规则。",
"写出你的加固版本，并用最小改动替换掉问题规则。",
"用计数器验证你的规则确实被命中。"],
 code:"# ❌ 问题规则集（请自行找出至少 6 个问题）\nsudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 3306 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 3389 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 6379 -j ACCEPT\nsudo iptables -A INPUT -j ACCEPT\nsudo iptables -P INPUT ACCEPT\n\n# 审计用的命令\nsudo iptables -L -n -v --line-numbers\nsudo iptables-save\n\n# ✅ 加固版本（参考）\nsudo iptables -F INPUT\nsudo iptables -A INPUT -i lo -j ACCEPT\nsudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\nsudo iptables -A INPUT -p tcp -s 10.0.0.0/24 --dport 22 -j ACCEPT\nsudo iptables -A INPUT -p tcp -s 10.0.1.0/24 --dport 3306 -j ACCEPT\nsudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT\nsudo iptables -A INPUT -m limit --limit 5/min -j LOG --log-prefix \"[FW-DROP] \"\nsudo iptables -A INPUT -j DROP\nsudo iptables -P INPUT DROP",
 verify:["能指出问题规则集里的：默认允许、无来源限制的管理端口、数据库/缓存端口全开、宽泛 ACCEPT 让后续规则失效、没有日志、没有出站控制","加固版本能正常 SSH（前提：把 10.0.0.0/24 换成你自己的网段）","加固版本的默认策略为 DROP，且日志规则在 DROP 之前"],
 undo:["实验环境专用：`sudo iptables -P INPUT ACCEPT && sudo iptables -F`"],
 warn:"请不要把「问题规则集」抄到任何真实服务器上——它会把 SSH、数据库、Redis 和 RDP 全部暴露出去。这个实验只在隔离的虚拟机里做。",
 why:"审计能力比配置能力更值钱。<b>配一台机器只需要记住语法，而看懂一份别人写的规则，需要你脑子里有一套完整的判断标准</b>——这套标准就是第 19 章那张清单。"
},
{
 title:"实验 11 · ufw / firewalld 与 iptables 的对照",
 meta:"难度 ★★☆　用时 25 分钟　环境：Ubuntu（ufw）+ CentOS/RHEL（firewalld），或只用 Ubuntu 做 ufw 部分",
 goal:"亲眼看到「友好前端」到底在底层生成了什么，从此不再把它们当成黑盒。",
 env:"Ubuntu 虚拟机（ufw 默认可用）；有 CentOS 虚拟机更好。",
 steps:["先在 ufw 关闭状态下记录一份 `iptables-save` 快照。",
"执行 `sudo ufw allow 22` 和 `sudo ufw allow 80`，但先不 enable。",
"再导出一次 `iptables-save`，对比两次的差异——看看 ufw 写了什么进去。",
"执行 `sudo ufw enable`，再次导出对比，观察它新增了哪些链和跳转规则。",
"用 `ufw status numbered` 查看规则编号，练习按编号删除。",
"（可选）在 CentOS 上用 firewalld 重复一遍，理解 zone 模型与 iptables 链的对应关系。"],
 code:"# ① 关闭状态下快照\nsudo ufw disable\nsudo iptables-save > /tmp/before.rules\n\n# ② 加规则但先不启用\nsudo ufw allow 22\nsudo ufw allow 80\n\n# ③ 看它写了什么\nsudo iptables-save > /tmp/after-add.rules\ndiff /tmp/before.rules /tmp/after-add.rules\n\n# ④ 启用后再看一次\nsudo ufw enable\nsudo iptables-save > /tmp/after-enable.rules\ndiff /tmp/after-add.rules /tmp/after-enable.rules\n\n# ⑤ 编号管理与删除\nsudo ufw status numbered\nsudo ufw delete 3\n\n# ⑥ firewalld（CentOS/RHEL）\nsudo firewall-cmd --get-active-zones\nsudo firewall-cmd --list-all\nsudo firewall-cmd --permanent --add-service=http && sudo firewall-cmd --reload\nsudo iptables-save | head -40       # 看底层链",
 verify:["diff 能清楚看到 ufw 往 iptables 里插入的链（如 ufw-user-input）和跳转规则","`ufw status numbered` 的顺序编号与实际生效顺序一致","firewalld 的 zone 在底层对应的是自定义链与跳转"],
 undo:["`sudo ufw disable`（实验后建议关闭，避免影响后续实验）","firewalld：`sudo firewall-cmd --permanent --remove-service=http && sudo firewall-cmd --reload`"],
 why:"很多人对 ufw / firewalld 有「黑盒焦虑」，出了问题只会重启服务。<b>看一次底层生成物，这种焦虑就消失了</b>：它们只是帮你写 iptables/nftables 规则的翻译器。"
},
{
 title:"实验 12 · 综合大实验：三网卡网关（WAN / LAN / DMZ）",
 meta:"难度 ★★★★　用时 2 小时　环境：一台三网卡 Linux 虚拟机 + 两台客户端",
 goal:"把这门课的全部知识点串成一个可运行的系统：NAT 上网、端口转发、内网隔离、日志审计。做完它，你就具备独立设计小型网络边界的能力。",
 env:"一台三网卡 Linux 虚拟机（WAN / LAN / DMZ），LAN 和 DMZ 各一台客户端。虚拟机网络模式务必用 NAT 或仅主机。开工前先打快照。",
 steps:["规划地址：WAN（DHCP 或固定）、LAN 192.168.10.0/24、DMZ 192.168.20.0/24。记录每块网卡的名字。",
"开启转发，并设默认策略：INPUT DROP、FORWARD DROP。",
"按顺序放行网关自身：lo、ESTABLISHED/RELATED、管理网段的 SSH。",
"实现 LAN 与 DMZ 访问外网：MASQUERADE 或 SNAT（POSTROUTING）。",
"放行 LAN → DMZ 的特定服务，<b>拒绝 DMZ → LAN 的一切</b>（这是安全域的核心约束）。",
"在 DMZ 客户端上起 Web 服务，用 DNAT 把 WAN 的 80/8080 映射到它。",
"加日志规则记录被拒绝的流量，并从 WAN 侧做一次端口扫描，观察日志里留下什么。",
"最后写一份「策略矩阵」文档：谁可以访问谁、走什么端口、依据是什么。"],
 code:"# 假设网卡：eth0=WAN  eth1=LAN 192.168.10.1/24  eth2=DMZ 192.168.20.1/24\n\n# ① 转发与默认策略\nsudo sysctl -w net.ipv4.ip_forward=1\nsudo iptables -P INPUT DROP\nsudo iptables -P FORWARD DROP\nsudo iptables -P OUTPUT ACCEPT\n\n# ② 网关自身保命规则（先放行，再收紧）\nsudo iptables -A INPUT -i lo -j ACCEPT\nsudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\nsudo iptables -A INPUT -i eth1 -p tcp --dport 22 -s 192.168.10.0/24 -j ACCEPT\n\n# ③ LAN / DMZ 出网上网\nsudo iptables -t nat -A POSTROUTING -s 192.168.10.0/24 -o eth0 -j MASQUERADE\nsudo iptables -t nat -A POSTROUTING -s 192.168.20.0/24 -o eth0 -j MASQUERADE\nsudo iptables -A FORWARD -i eth1 -o eth0 -j ACCEPT\nsudo iptables -A FORWARD -i eth2 -o eth0 -j ACCEPT\nsudo iptables -A FORWARD -i eth0 -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\n\n# ④ LAN → DMZ 只放行必要服务；DMZ → LAN 一律拒绝\nsudo iptables -A FORWARD -i eth1 -o eth2 -p tcp --dport 80 -j ACCEPT\nsudo iptables -A FORWARD -i eth1 -o eth2 -p tcp --dport 443 -j ACCEPT\nsudo iptables -A FORWARD -i eth2 -o eth1 -j LOG --log-prefix \"[DMZ-TO-LAN] \"\nsudo iptables -A FORWARD -i eth2 -o eth1 -j DROP\n\n# ⑤ 端口转发：WAN 8080 → DMZ Web\nsudo iptables -t nat -A PREROUTING -i eth0 -p tcp --dport 8080 -j DNAT --to-destination 192.168.20.10:80\nsudo iptables -A FORWARD -i eth0 -o eth2 -p tcp -d 192.168.20.10 --dport 80 -j ACCEPT\n\n# ⑥ 日志与审计\nsudo iptables -A FORWARD -m limit --limit 10/min -j LOG --log-prefix \"[FW-FWD-DROP] \"\nsudo iptables -A INPUT -m limit --limit 10/min -j LOG --log-prefix \"[FW-IN-DROP] \"\nsudo journalctl -k -f | grep FW-\n\n# ⑦ 验证关键路径\n#    LAN 客户端: curl http://192.168.20.10   → 应成功（80 已放行）\n#    DMZ 客户端: curl http://192.168.10.x   → 应失败（被 DROP 并记录日志）\n#    外网:       curl http://WAN_IP:8080     → 应成功\n\n# ⑧ 保存成果\nsudo iptables-save > /root/gateway.rules",
 verify:["LAN 客户端能访问外网（MASQUERADE 生效）","LAN 客户端能访问 DMZ 的 80/443，但不能 SSH 进 DMZ 的其他端口","<b>DMZ 客户端无法访问 LAN 的任何服务</b>（这是本实验最重要的验证点）","外网能通过 WAN:8080 访问到 DMZ 的 Web 服务","从 WAN 扫描端口时，日志里能看到 [FW-IN-DROP] 记录"],
 undo:["实验后按快照恢复，或执行 `sudo iptables -F && sudo iptables -t nat -F && sudo iptables -P INPUT ACCEPT && sudo iptables -P FORWARD ACCEPT`","`sudo sysctl -w net.ipv4.ip_forward=0`"],
 warn:"三网卡的多网段实验一定要用虚拟机的 NAT / 仅主机网络，绝对不要桥接到公司或学校的真实网络——错误的转发配置可能把实验网络变成开放代理。",
 why:"这个实验是把课程全部知识点串起来的地方：<b>安全域设计、默认拒绝、有状态放行、NAT、端口转发、隔离、日志审计</b>。能独立完成并解释每一步理由，你就达到了「精通自测清单」要求的实战水平。"
}
];
