// MySQL 学习站 v2 · 实验手册
var LABS = [
{
 title:"实验 1 · 装好 XAMPP 并建出练习库",
 meta:"难度 ★☆☆　用时 15 分钟　环境：你自己的 Windows",
 goal:"把环境跑通，并且理解 XAMPP 的 mysql 命令为什么一开始敲不出来。",
 env:"Windows 电脑，能装 XAMPP（约 150 MB）。全程只在本机操作。",
 steps:["安装 XAMPP，启动控制面板里的 <b>MySQL</b> 服务（Apache 可选）。",
"打开 cmd，直接敲 `mysql -u root` —— <b>故意先踩一次坑</b>，观察报错（提示不是内部或外部命令）。",
"进入 XAMPP 的 mysql 目录：`cd C:\\xampp\\mysql\\bin`，再敲 `mysql -u root` 就能进来了。",
"在 code 文件夹执行 init.sql 建库：`mysql -u root < init.sql`。",
"进入命令行确认：`SHOW DATABASES;`、`USE shop;`、`SHOW TABLES;`、`SELECT * FROM user;`。"],
code:":: ① 先确认 mysql 命令在哪（XAMPP 装完默认不在 PATH）\ndir C:\\xampp\\mysql\\bin\n\n:: ② 进入该目录后执行建库脚本（init.sql 在课程文件夹的 code\\ 下）\ncd /d C:\\xampp\\mysql\\bin\nmysql -u root < 你放 init.sql 的路径\\init.sql\n\n:: ③ 进命令行验证\nmysql -u root\nSHOW DATABASES;\nUSE shop;\nSHOW TABLES;\nSELECT * FROM user;\nSELECT * FROM orders;\nSELECT * FROM product;",
 verify:["`SHOW DATABASES;` 里能看到 shop","`SHOW TABLES;` 至少列出 user / orders / product / demo2 / admin_log 五张表","`SELECT COUNT(*) FROM user;` 返回一个大于 0 的数字","重启电脑后 MySQL 依然能连上（说明服务是常驻的）"],
 undo:["想清空重来：`DROP DATABASE shop;` 然后重新跑一次 init.sql"],
 why:"这个实验最重要的不是装软件，而是理解「命令找不到 ≠ 软件没装」。环境问题占了新手卡壳的一大半，遇到过、解决过一次之后，同类问题就再也不会卡住你。"
},
{
 title:"实验 2 · 表设计：把约束踩出感觉",
 meta:"难度 ★★☆　用时 25 分钟　环境：XAMPP + shop 库",
 goal:"亲手体验约束的作用——它不只是规范，而是数据库自带的防线。",
 env:"实验 1 已完成，shop 库已建好。",
 steps:["自己设计一张 `member` 表：用户名唯一、邮箱唯一、年龄必须在 18-100 之间、状态只能是三个固定值之一。",
"插入一条正常数据，确认成功。",
"<b>故意插入重复用户名</b>，观察 UNIQUE 约束报错。",
"<b>故意插入超范围年龄</b>，观察 CHECK 约束报错。",
"<b>故意插入非法状态值</b>，观察 CHECK 约束报错。",
"再故意写一个超长用户名（超过 VARCHAR 长度），观察严格模式下是报错还是被截断。"],
 code:"CREATE TABLE member (\n  id       INT AUTO_INCREMENT PRIMARY KEY,\n  username VARCHAR(30) NOT NULL UNIQUE,\n  email    VARCHAR(100) NOT NULL UNIQUE,\n  age      TINYINT UNSIGNED NOT NULL CHECK (age BETWEEN 18 AND 100),\n  status   VARCHAR(10) NOT NULL DEFAULT 'active'\n           CHECK (status IN ('active','frozen','banned')),\n  created_at DATETIME DEFAULT CURRENT_TIMESTAMP\n) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;\n\n-- 正常插入\nINSERT INTO member (username, email, age) VALUES ('tester', 't@example.com', 25);\n\n-- ① 重复用户名（应当报错 Duplicate entry）\nINSERT INTO member (username, email, age) VALUES ('tester', 'other@example.com', 26);\n\n-- ② 年龄超范围（应当报错 Check constraint violated）\nINSERT INTO member (username, email, age) VALUES ('young', 'y@example.com', 15);\n\n-- ③ 非法状态（应当报错）\nINSERT INTO member (username, email, status) VALUES ('bad', 'b@example.com', 'whatever');\n\n-- ④ 超长用户名（看严格模式是报错还是截断）\nINSERT INTO member (username, email, age) VALUES (REPEAT('x', 50), 'long@example.com', 30);\nSELECT username, LENGTH(username) FROM member WHERE email = 'long@example.com';\n\n-- ⑤ 看看当前 sql_mode 到底是不是严格模式\nSELECT @@sql_mode;",
 verify:["重复用户名被拒绝，报 Duplicate entry","年龄 15 被拒绝，报 CHECK 约束错误","非法状态值被拒绝","超长用户名在严格模式下直接报错（如果没报错，说明你的 sql_mode 少了 STRICT_TRANS_TABLES）","`SELECT @@sql_mode` 能看到 STRICT_TRANS_TABLES"],
 undo:["`DROP TABLE member;`"],
 why:"亲手被约束拦几次，比读十遍定义都管用。更重要的是第 ④ 步：如果超长数据被静默截断了，你就亲眼看到了「没有严格模式会制造出意料之外的数据」——这是审计时必查的一项。"
},
{
 title:"实验 3 · 增删改与保命开关",
 meta:"难度 ★★☆　用时 20 分钟　环境：shop 库",
 goal:"体会 UPDATE / DELETE 的「影响面」，并学会用 SQL_SAFE_UPDATES 给自己上保险。",
 env:"shop 库（建议先 `mysqldump` 备一份，见实验 10）。",
 steps:["在 demo2 表里插入 100 条测试数据。",
"关掉安全更新模式，执行一次不带 WHERE 的 UPDATE，观察有多少行被改（affected rows）。",
"把数据恢复，再开启安全更新模式，重复同样的语句，观察被拒绝。",
"用「先 SELECT 后 DELETE」的习惯删掉其中 10 条，确认删除的正是想删的那些行。",
"最后验证：`SELECT COUNT(*)` 的数字和你预期的是否一致。"],
 code:"-- ① 造 100 条数据（MySQL 8.0 用递归 CTE）\nINSERT INTO demo2 (name)\nWITH RECURSIVE seq AS (\n  SELECT 1 AS n UNION ALL SELECT n+1 FROM seq WHERE n < 100\n)\nSELECT CONCAT('item-', n) FROM seq;\nSELECT COUNT(*) FROM demo2;\n\n-- ② 关掉安全模式，执行危险操作（注意看 affected rows）\nSET SQL_SAFE_UPDATES = 0;\nUPDATE demo2 SET name = 'x';            -- 100 行全被改！\nSELECT COUNT(*) FROM demo2 WHERE name = 'x';\n\n-- ③ 恢复数据\nTRUNCATE TABLE demo2;\nINSERT INTO demo2 (name)\nWITH RECURSIVE seq AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM seq WHERE n < 100)\nSELECT CONCAT('item-', n) FROM seq;\n\n-- ④ 开启安全模式，同样的语句应该被拒绝\nSET SQL_SAFE_UPDATES = 1;\nUPDATE demo2 SET name = 'y';            -- 应当报错拒绝\n\n-- ⑤ 正确姿势：先 SELECT 看范围，再 DELETE\nSELECT * FROM demo2 WHERE id <= 10;     -- 先确认这 10 行是你要删的\nDELETE FROM demo2 WHERE id <= 10;       -- 再删\nSELECT COUNT(*) FROM demo2;             -- 应为 90",
 verify:["② 的 affected rows 是 100（不是 1）——这就是「没有 WHERE」的真实后果","④ 在安全模式下被拒绝执行","⑤ 删除后 `SELECT COUNT(*)` 正好是 90","能说清 SQL_SAFE_UPDATES 只防「WHERE 不含主键/索引」的语句，不是万能保险"],
 undo:["`TRUNCATE TABLE demo2;` 然后重新插入数据，或直接从 init.sql 重建 demo2"],
 warn:"这是唯一一个「故意做危险操作」的实验，请确认你只在 <b>本地 XAMPP 的 demo2 表</b> 上做。绝对不要在任何真实数据库上尝试不带 WHERE 的 UPDATE/DELETE。",
 why:"「affected rows 是 100 而不是 1」这个瞬间，比任何警告文字都有说服力。以后你写 UPDATE 时，脑子里会自动闪过这个数字。"
},
{
 title:"实验 4 · 查询执行顺序：一句话解释所有怪报错",
 meta:"难度 ★☆☆　用时 15 分钟　环境：shop 库",
 goal:"用几个报错亲身体会「书写顺序 ≠ 执行顺序」，从此不再被 Unknown column 搞晕。",
 env:"shop 库。",
 steps:["先试一个在 WHERE 里用别名的查询，观察报错。",
"把同样的别名用到 ORDER BY 里，观察能成功。",
"用 HAVING 引用 SELECT 里的聚合别名，观察能成功。",
"用 WHERE 引用聚合函数，观察报错。",
"最后在纸上或脑子里默写一遍执行顺序，再去讲给别人听。"],
 code:"-- ① WHERE 里用别名 → 报错\nSELECT username AS n FROM user WHERE n = 'admin';\n\n-- ② ORDER BY 里用别名 → 成功（因为它在 SELECT 之后执行）\nSELECT username AS n FROM user ORDER BY n DESC;\n\n-- ③ HAVING 里用聚合别名 → 成功\nSELECT role, COUNT(*) AS cnt FROM user GROUP BY role HAVING cnt >= 1;\n\n-- ④ WHERE 里用聚合函数 → 报错\nSELECT * FROM user WHERE COUNT(*) > 1;\n\n-- ⑤ 完整的执行顺序（背下来）\n-- FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT",
 verify:["① 报 Unknown column 'n' in 'where clause'","② 正常返回结果","③ 正常返回分组统计","④ 报 Invalid use of group function","你能不看笔记说出执行顺序，并解释为什么 HAVING 能用聚合函数而 WHERE 不能"],
 undo:["纯查询实验，无需回滚"],
 why:"这个知识点性价比极高：理解执行顺序之后，你对 WHERE / HAVING 的区别、别名能不能用、聚合函数放哪里，都能自己推出来，不用死记。"
},
{
 title:"实验 5 · 聚合与 GROUP_CONCAT 的截断陷阱",
 meta:"难度 ★★☆　用时 20 分钟　环境：shop 库",
 goal:"亲眼看到 GROUP_CONCAT 被 1024 字节截断，理解为什么注入时经常「只拿到一半数据」。",
 env:"shop 库，建议先往 user 表多插一些数据。",
 steps:["先用少量数据看 GROUP_CONCAT 正常拼接的效果。",
"批量插入几十条较长的记录。",
"再次 GROUP_CONCAT，观察结果长度——会发现它被卡在 1024 附近。",
"查看并调大 group_concat_max_len，重新拼接，数据就完整了。",
"对比两次结果的长度差，理解「截断」是静默发生的。"],
 code:"-- ① 先看正常效果\nSELECT GROUP_CONCAT(username) FROM user;\nSELECT GROUP_CONCAT(CONCAT(username, ':', password)) FROM user;\n\n-- ② 造足够多的数据（用一个较长字段撑大结果）\nDROP TABLE IF EXISTS bigtext;\nCREATE TABLE bigtext (id INT AUTO_INCREMENT PRIMARY KEY, v VARCHAR(500));\nINSERT INTO bigtext (v)\nWITH RECURSIVE seq AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM seq WHERE n < 200)\nSELECT REPEAT('A', 100) FROM seq;\n\n-- ③ 拼接，观察长度被卡住\nSELECT LENGTH(GROUP_CONCAT(v)) AS 结果长度 FROM bigtext;      -- 大概率是 1024\n\n-- ④ 看上限并调大\nSHOW VARIABLES LIKE 'group_concat_max_len';\nSET SESSION group_concat_max_len = 1000000;\n\n-- ⑤ 重新拼接，长度明显变大\nSELECT LENGTH(GROUP_CONCAT(v)) AS 调整后长度 FROM bigtext;",
 verify:["③ 的结果长度正好卡在 1024（或该变量的当前值）","⑤ 调整后长度明显变大（接近 200×100=20000）","你能解释：为什么攻击者拖库时会执行 SET SESSION group_concat_max_len","你能说出这条 SET 语句是审计日志里的注入特征之一"],
 undo:["`DROP TABLE bigtext;`"],
 tip:"这不是 MySQL 的 bug，而是默认配置的取舍：不限长度可能一次返回几百 MB 把客户端撑爆。但作为安全意识，你必须知道它<b>会静默截断</b>——「拿到一半的数据还以为拿全了」是很危险的判断。"
},
{
 title:"实验 6 · JOIN 与子查询：找出没下过单的用户",
 meta:"难度 ★★☆　用时 25 分钟　环境：shop 库",
 goal:"掌握 INNER / LEFT JOIN 的结果差异，以及「找缺失记录」的经典写法。",
 env:"shop 库。先在 user 表里加一个没有任何订单的用户。",
 steps:["先插一个没订单的用户（例如 dave）。",
"用 INNER JOIN 查「用户 + 订单」，观察 dave 消失了。",
"改用 LEFT JOIN，观察 dave 出现但订单列为 NULL。",
"用 LEFT JOIN + IS NULL 精确找出没下单的用户。",
"用子查询写一遍同样的需求，对比两种写法的可读性。"],
 code:"-- ① 造一个没有订单的用户\nINSERT INTO user (username, password, email, role)\nVALUES ('dave', 'e10adc3949ba59abbe56e057f20f883e', 'dave@example.com', 'user');\n\n-- ② INNER JOIN：dave 不见了\nSELECT u.username, o.id AS 订单号, o.amount\nFROM user u\nINNER JOIN orders o ON o.user_id = u.id;\n\n-- ③ LEFT JOIN：dave 在，订单列为 NULL\nSELECT u.username, o.id AS 订单号, o.amount\nFROM user u\nLEFT JOIN orders o ON o.user_id = u.id\nORDER BY u.username;\n\n-- ④ 精确找出「没有订单的用户」\nSELECT u.username\nFROM user u\nLEFT JOIN orders o ON o.user_id = u.id\nWHERE o.id IS NULL;\n\n-- ⑤ 用子查询实现同一个需求\nSELECT username FROM user\nWHERE id NOT IN (SELECT DISTINCT user_id FROM orders);\n\n-- ⑥ 三表 JOIN：订单明细\nSELECT o.id AS 订单号, u.username AS 用户, p.product_name AS 商品, o.amount\nFROM orders o\nJOIN user u    ON o.user_id = u.id\nJOIN product p ON o.product_id = p.id\nORDER BY o.id;",
 verify:["② 结果里没有 dave（INNER JOIN 把没订单的人过滤掉了）","③ 结果里有 dave，且订单号和金额为 NULL","④ 结果里包含 carol（init.sql 里故意没给她订单）和新插入的 dave","⑤ 子查询结果与 ④ 完全一致","⑥ 三表 JOIN 能正确显示商品名（如果报 Unknown column，说明 init.sql 不是最新版——重新跑一次）"],
 undo:["`DELETE FROM user WHERE username='dave';`（注意订单表有外键，需先删它的订单或按 1 号用户的写法处理）"],
 why:"「找出没有 X 的记录」是面试和实际工作里的高频需求。LEFT JOIN + IS NULL 是最经典的解法，而它背后的思维——<b>用一个连接去找「匹配不上的那些」</b>——在很多场景都能用上。"
},
{
 title:"实验 7 · 索引实验：让 EXPLAIN 亲眼说服你",
 meta:"难度 ★★★　用时 30 分钟　环境：shop 库",
 goal:"造出足够多的数据，用 EXPLAIN 对比加索引前后的执行计划，理解索引到底改变了什么。",
 env:"shop 库。需要 MySQL 8.0（用递归 CTE 造数据）。",
 steps:["造一张 10 万行的表。",
"在没有索引的情况下 EXPLAIN 一个条件查询，记录 type 和 rows。",
"建索引后再 EXPLAIN 同一条查询，对比 type、key、rows 的变化。",
"测试四种「索引失效」的写法，逐个观察执行计划退化。",
"最后测一次复合索引的最左前缀行为。"],
 code:"-- ① 造 10 万行数据\nDROP TABLE IF EXISTS big;\nCREATE TABLE big (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  uid INT NOT NULL,\n  name VARCHAR(50) NOT NULL,\n  created_at DATETIME NOT NULL,\n  INDEX idx_uid (uid)\n) ENGINE=InnoDB;\n\nINSERT INTO big (uid, name, created_at)\nWITH RECURSIVE seq AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM seq WHERE n < 100000)\nSELECT n % 1000, CONCAT('user-', n), NOW() - INTERVAL n SECOND FROM seq;\n\nSELECT COUNT(*) FROM big;\n\n-- ② 未命中索引的查询（uid 有索引，但这里按 name 查）\nEXPLAIN SELECT * FROM big WHERE name = 'user-500';\\G\n--   关注 type（应为 ALL 全表扫描）和 rows（应接近 100000）\n\n-- ③ 给 name 建索引，再解释一次\nCREATE INDEX idx_name ON big(name);\nEXPLAIN SELECT * FROM big WHERE name = 'user-500';\\G\n--   type 应变成 ref，rows 应降到个位数\n\n-- ④ 四种索引失效的写法，逐个对比\nEXPLAIN SELECT * FROM big WHERE YEAR(created_at) = 2026;\\G          -- 函数运算 → ALL\nEXPLAIN SELECT * FROM big WHERE created_at >= '2026-01-01';\\G        -- 范围 → range（走索引）\nEXPLAIN SELECT * FROM big WHERE name LIKE '%500%';\\G                 -- 前导通配 → ALL\nEXPLAIN SELECT * FROM big WHERE uid = '1';\\G                         -- 隐式转换（uid 是 INT）\n\n-- ⑤ 复合索引的最左前缀\nALTER TABLE big ADD INDEX idx_uid_name (uid, name);\nEXPLAIN SELECT * FROM big WHERE uid = 1 AND name = 'user-500';\\G     -- 走复合索引\nEXPLAIN SELECT * FROM big WHERE name = 'user-500';\\G                 -- 跳过最左列",
 verify:["③ 的 type 从 ALL 变成 ref，rows 从十万级降到个位数","④ 中「函数运算」和「前导通配」两条都退化为 ALL","④ 中「范围查询」能走索引（type=range）","⑤ 能看出复合索引对「跳过最左列」的查询失效","你能用自己的话说清：索引改变的是「找数据的方式」，从逐行比对变成按树定位"],
 undo:["`DROP TABLE big;`（十几万行，删掉能回收空间）"],
 warn:"这个实验会造 10 万行数据，占用几十 MB 磁盘和几分钟时间。做完建议直接 DROP TABLE 清理。",
 why:"索引是那种「讲一百遍不如看一眼」的知识点。当你看到 rows 从 100000 掉到 1 的那一刻，就再也不会忘记索引做什么了。而四种失效写法，是面试和实际优化里的高频考点。"
},
{
 title:"实验 8 · 事务与锁：亲眼看到「锁全表」",
 meta:"难度 ★★★　用时 35 分钟　环境：shop 库 + 两个 MySQL 会话",
 goal:"理解为什么「UPDATE 不带索引条件」会锁住整张表，并学会观察锁等待。",
 env:"需要同时开两个 mysql 命令行窗口（会话 A 和会话 B）。",
 steps:["准备一张表，一个字段有索引、一个字段没有。",
"会话 A 开启事务，用<b>无索引</b>的字段做条件更新，先不提交。",
"会话 B 尝试更新另一行（用主键），观察它被阻塞、一直等待。",
"在第三个窗口查锁等待情况。",
"会话 A 提交后，观察会话 B 立刻恢复。",
"换成<b>有索引</b>的字段重复一次，观察会话 B 不再被阻塞。"],
 code:"-- 准备（任一会话执行）\nDROP TABLE IF EXISTS locktest;\nCREATE TABLE locktest (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  uid INT NOT NULL,\n  status VARCHAR(20) NOT NULL          -- 故意不给 status 建索引\n) ENGINE=InnoDB;\nINSERT INTO locktest (uid, status) VALUES (1,'a'),(2,'a'),(3,'a'),(4,'a'),(5,'a');\n\n-- ===== 会话 A =====\nSTART TRANSACTION;\nUPDATE locktest SET status = 'b' WHERE status = 'a';     -- 无索引条件，锁很多行\n-- 先不要 COMMIT\n\n-- ===== 会话 B（此时执行，会卡住）=====\nUPDATE locktest SET status = 'c' WHERE id = 5;\n-- 观察：一直等待，直到超时或会话 A 提交\n\n-- ===== 第三个窗口：查看锁等待 =====\nSELECT * FROM performance_schema.data_lock_waits;\nSELECT trx_id, trx_state, trx_query, trx_mysql_thread_id\nFROM information_schema.innodb_trx;\n\n-- ===== 会话 A：提交，B 立刻恢复 =====\nCOMMIT;\n\n-- ===== 对照实验：给 status 建索引后再试一次 =====\nCREATE INDEX idx_status ON locktest(status);\n-- 重复上面的 A/B 步骤，观察 B 不再被长时间阻塞",
 verify:["会话 B 的 UPDATE 明显被阻塞（不返回结果）","`performance_schema.data_lock_waits` 能看到等待关系","会话 A COMMIT 后，会话 B 立即完成","给 status 建索引后，同样的场景不再长时间阻塞","你能解释：InnoDB 行锁加在索引上，没有可用索引时扫过的行都会被锁"],
 undo:["`DROP TABLE locktest;`（如果还有未提交事务，先 COMMIT 或 ROLLBACK）"],
 warn:"如果会话 A 忘记 COMMIT，会话 B 会一直等下去（默认 50 秒后超时）。做完记得提交或回滚，别把窗口丢在那儿。",
 tip:"这个实验解释了一类很难查的线上故障：<b>一条「只改几行」的语句让整个服务卡住</b>。以后遇到这类问题，第一反应就应该是看那条语句的 WHERE 走没走索引。"
},
{
 title:"实验 9 · 复现一次死锁并读懂日志",
 meta:"难度 ★★★★　用时 30 分钟　环境：两个 MySQL 会话",
 goal:"亲手制造一次死锁，学会用 SHOW ENGINE INNODB STATUS 读死锁现场。",
 env:"两个 mysql 会话，一张有两行数据的表。",
 steps:["建一张简单表并插入两行。",
"会话 A 先锁第 1 行，会话 B 先锁第 2 行。",
"会话 A 再尝试锁第 2 行，会话 B 再尝试锁第 1 行——<b>死锁产生</b>。",
"观察哪一方被 InnoDB 回滚（通常是影响较小的那个）。",
"用 SHOW ENGINE INNODB STATUS 找到 LATEST DETECTED DEADLOCK，逐行读懂两个事务各自持有什么锁、在等什么。",
"最后按「固定访问顺序」重写一遍，验证死锁不再出现。"],
 code:"-- 准备\nDROP TABLE IF EXISTS deadlocktest;\nCREATE TABLE deadlocktest (id INT PRIMARY KEY, v INT) ENGINE=InnoDB;\nINSERT INTO deadlocktest VALUES (1,0),(2,0);\n\n-- ===== 步骤 1 =====\n-- 会话 A:\nSTART TRANSACTION;\nUPDATE deadlocktest SET v = v+1 WHERE id = 1;\n\n-- 会话 B:\nSTART TRANSACTION;\nUPDATE deadlocktest SET v = v+1 WHERE id = 2;\n\n-- ===== 步骤 2（交叉等待 → 死锁）=====\n-- 会话 A:  （会卡住）\nUPDATE deadlocktest SET v = v+1 WHERE id = 2;\n\n-- 会话 B:  （触发死锁检测）\nUPDATE deadlocktest SET v = v+1 WHERE id = 1;\n-- 结果：其中一方报 Deadlock found when trying to get lock; try restarting transaction\n\n-- ===== 步骤 3：读死锁日志 =====\nSHOW ENGINE INNODB STATUS\\G\n-- 找到 LATEST DETECTED DEADLOCK 段落，回答三个问题：\n--   ① 哪个事务被回滚了？\n--   ② 两个事务分别持有什么锁、在等什么锁？\n--   ③ 它们操作的是哪几行？\n\n-- ===== 步骤 4：修复写法（统一访问顺序）=====\n-- 约定：所有事务都按 id 从小到大更新\n-- 会话 A: UPDATE ... WHERE id = 1; 然后 UPDATE ... WHERE id = 2;\n-- 会话 B: UPDATE ... WHERE id = 1; 然后 UPDATE ... WHERE id = 2;\n-- 这样 B 会在第一步就排队等待，而不是交叉持锁",
 verify:["两个会话交叉更新后，其中一个报 Deadlock found","`SHOW ENGINE INNODB STATUS` 能看到 LATEST DETECTED DEADLOCK 段落","你能回答日志里的三个问题（谁被回滚、各自持有什么锁、操作哪几行）","改成统一访问顺序后，重复实验不再出现死锁"],
 undo:["`DROP TABLE deadlocktest;`，并确保两个会话都已 COMMIT 或 ROLLBACK"],
 why:"死锁不是「数据库有问题」，而是并发访问顺序不一致的必然结果。它最重要的认知是：<b>死锁无法完全避免，但可以用「统一访问顺序 + 短事务 + 走索引」把概率降到极低</b>。而会读死锁日志，是你能不能自己解决这类问题的分水岭。"
},
{
 title:"实验 10 · 备份与恢复演练（最重要的一次练习）",
 meta:"难度 ★★★　用时 30 分钟　环境：XAMPP + shop 库",
 goal:"完整走一遍「备份 → 模拟误删 → 恢复 → 验证」，并亲眼看到 binlog 的作用。",
 env:"shop 库（确保已跑过 init.sql）。",
 steps:["用 mysqldump 做一次全量备份，确认文件有内容。",
"模拟事故：删掉 orders 表里的几条数据。",
"用备份恢复，验证数据回来了。",
"检查 log_bin 是否开启（开启才有时间点恢复能力）。",
"最后回答三个问题：多久备一次？有没有异地副本？多久演练一次恢复？"],
 code:":: ① 全量备份（务必加 --single-transaction）\ncd /d C:\\xampp\\mysql\\bin\nmysqldump -u root --single-transaction --routines --triggers shop > shop_backup.sql\n\n:: ② 确认文件真的有内容（不要只看命令有没有报错）\ndir shop_backup.sql\nfindstr /C:\"CREATE TABLE\" shop_backup.sql\n\n:: ③ 模拟事故：删掉部分数据\nmysql -u root shop -e \"DELETE FROM orders WHERE id <= 2;\"\nmysql -u root shop -e \"SELECT COUNT(*) AS 删除后订单数 FROM orders;\"\n\n:: ④ 恢复\nmysql -u root shop < shop_backup.sql\nmysql -u root shop -e \"SELECT COUNT(*) AS 恢复后订单数 FROM orders;\"\n\n:: ⑤ 看 binlog 是否开启\nmysql -u root -e \"SHOW VARIABLES LIKE 'log_bin'; SHOW BINARY LOGS;\「",
 verify:["② 备份文件大小 > 0，且能搜到 CREATE TABLE 语句","③ 删除后 COUNT 变小","④ 恢复后 COUNT 回到删除前的数字","你能解释 --single-transaction 为什么能保证一致性","你能回答备份策略的三个问题，并给出具体答案（例如：每天全量 + 异地 + 每季度演练）"],
 undo:["实验本身就是恢复演练，做完数据已还原。若想彻底重来：`DROP DATABASE shop;` 后重跑 init.sql"],
 why:"这是整个实验手册里最值钱的一个。<b>「备份脚本跑了半年，真出事时才发现文件是空的」</b>是运维事故里最经典的悲剧。亲手做一次恢复，你对备份的理解会从「知道有这回事」变成「知道它靠不靠得住」。"
},
{
 title:"实验 11 · 注入判定与 UNION 拖库（本地靶场）",
 meta:"难度 ★★★★　用时 40 分钟　环境：你自己的 shop 库 + 本地靶场页面",
 goal:"先在没有回显干扰的 SQL 层面理解注入的每一步在做什么，再用 DVWA 之类的本地靶场做一次真实的 HTTP 注入。",
 env:"shop 库 + 本机搭的靶场（DVWA / 自己写的最简查询页面）。<b>全程只在本机。</b>",
 steps:["在 SQL 层面模拟「拼接」：先写正常查询，再写注入后等价的那条语句，对比结果差异。",
"用 demo2 表练探列数：ORDER BY 1 → 2 → 3，观察报错的位置。",
"用 information_schema 手工完成「库 → 表 → 列」的枚举。",
"用 GROUP_CONCAT 一次性取回 user 表的用户名与密码哈希。",
"打开本地 DVWA（把安全级别设为 Low），在 SQL Injection 页面做一次真实的 HTTP 注入，感受「URL 参数 → 数据库」的完整链路。",
"记录：可控参数、注入类型、验证方式——这份记录就是修复后的复测清单。"],
 code:"-- ① 拼接与注入的对照（SQL 层面理解）\nSELECT * FROM user WHERE username = 'admin';            -- 正常\nSELECT * FROM user WHERE username = 'admin' OR '1'='1';  -- 条件恒真 → 返回全部\nSELECT * FROM user WHERE username = 'admin' -- ';         -- 后面被注释 → 绕过密码校验\n\n-- ② 探列数（demo2 只有 2 列）\nSELECT * FROM demo2 ORDER BY 1;   -- 正常\nSELECT * FROM demo2 ORDER BY 2;   -- 正常\nSELECT * FROM demo2 ORDER BY 3;   -- 报错 → 说明只有 2 列\n\n-- ③ 用 information_schema 枚举结构\nSELECT DATABASE();\nSELECT table_name FROM information_schema.tables WHERE table_schema = DATABASE();\nSELECT column_name FROM information_schema.columns\nWHERE table_schema = 'shop' AND table_name = 'user';\n\n-- ④ 把用户名和密码哈希一次取回\nSELECT GROUP_CONCAT(CONCAT(username, ':', password)) AS 结果 FROM user;\n\n-- ⑤ 把结果长度撑到上限，观察截断（配合实验 5 理解）\nSHOW VARIABLES LIKE 'group_concat_max_len';",
 verify:["① 你能指出哪一条是「绕过登录」、哪一条是「返回全部数据」，并说清区别","② 你能根据报错准确说出表有几列","③ 你能完整列出 shop 库的表和 user 表的列","④ 你能取回全部用户名与哈希","⑤ 在 DVWA 上你能独立完成一次判定并说明用的是哪种注入类型"],
 undo:["SQL 层面无需回滚（都是查询）；DVWA 在容器/本地环境，重置数据库即可"],
 warn:"<b>红线：只在你自己的本地靶场（XAMPP + shop 库 + DVWA）执行。</b>对任何真实网站、学校系统、公司数据库做同样的操作都属于违法行为，无论是否造成损失、无论出于什么目的。这里学的每一步，都是为了让你在审计代码时认出来、修掉它。",
 why:"在 SQL 层面先理解每一步，再去 HTTP 层面打一次真实靶场——这个顺序很重要。如果一上来就用 sqlmap 打靶场，你会「会用工具」但「不懂原理」，遇到变种就束手无策。"
},
{
 title:"实验 12 · 综合实验：给自己的项目做一次数据库加固审计",
 meta:"难度 ★★★★★　用时 1.5 小时　环境：XAMPP + 任意一个你写过的项目库",
 goal:"把第 17-19 章的加固清单真正落到一个真实项目上，产出一份可执行的审计报告。",
 env:"你自己的某个项目（课设、博客、练手站都行），或者就用 shop 库。",
 steps:["<b>摸清现状</b>：依次执行下面那段自查 SQL，把结果记下来。",
"<b>网络层</b>：确认 3306 没有对公网开放（本机场景确认 bind-address）。",
"<b>权限层</b>：列出所有账号及其全局权限，找出「应用在用 root」和「有多余 FILE/DROP 权限」的账号。",
"<b>配置层</b>：检查 secure_file_priv、local_infile、sql_mode、log_bin 四项。",
"<b>数据层</b>：检查密码字段是不是哈希、哈希算法是不是 bcrypt/Argon2（不是 MD5）。",
"<b>产出报告</b>：写成一页纸——现状、风险等级、修复动作、验证方式。修复后再逐条复测。"],
 code:"-- ① 账号与权限审计\nSELECT user, host, plugin FROM mysql.user ORDER BY user;\nSELECT grantee, privilege_type FROM information_schema.user_privileges\nWHERE privilege_type IN ('FILE','SUPER','GRANT OPTION','CREATE USER','SHUTDOWN','PROCESS');\nSHOW GRANTS FOR CURRENT_USER();\n\n-- ② 配置审计\nSHOW VARIABLES WHERE Variable_name IN (\n  'version','bind_address','port','secure_file_priv','local_infile',\n  'sql_mode','max_connections','log_bin','datadir','plugin_dir');\n\n-- ③ 数据审计：看密码列存的什么\nSELECT username, role, LENGTH(password) AS 哈希长度, LEFT(password, 8) AS 前八位 FROM user;\n--   32 位 → 很可能是 MD5（不安全）\n--   60 位 → 可能是 bcrypt（推荐）\n\n-- ④ 约束审计：看表有没有主键、外键、唯一约束\nSELECT table_name, column_name, column_key, is_nullable, data_type\nFROM information_schema.columns\nWHERE table_schema = 'shop' ORDER BY table_name, ordinal_position;\n\n-- ⑤ 结构审计：表、索引、视图、过程、触发器都要看\nSELECT table_name, table_type FROM information_schema.tables WHERE table_schema='shop';\nSELECT table_name, index_name, column_name, non_unique FROM information_schema.statistics WHERE table_schema='shop';\nSELECT ROUTINE_NAME, DEFINER FROM information_schema.routines WHERE ROUTINE_SCHEMA='shop';\nSELECT TRIGGER_NAME, DEFINER FROM information_schema.triggers WHERE TRIGGER_SCHEMA='shop';\n\n-- ⑥ 工具：MySQL 自带的加固脚本（按提示回答即可）\n-- mysql_secure_installation",
 verify:["你能说出「哪些账号有全局权限、它们是不是必需的」","你能指出 secure_file_priv 当前的取值意味着什么","你能判断密码列的哈希算法是否安全","你能列出所有应用账号里权限过大的那一个","你产出了一页纸的审计报告，含现状 / 风险 / 修复 / 验证四部分","修复后逐条复测通过"],
 undo:["审计本身是只读操作；如果执行了 mysql_secure_installation，按你的记忆恢复相应配置（例如 root 口令）"],
 warn:"如果你审计的是<b>公司或学校的真实系统</b>，请只做只读检查，任何修改都要先取得授权并走变更流程。这个实验默认的练习对象是你自己的项目或 shop 库。",
 why:"这是整门课的收尾实验：把「注入防御、权限最小化、配置加固、备份验证」落到一个真实项目上。<b>能独立完成这份审计报告，你就达到了首页自测清单要求的水平</b>——不是会写 SQL，而是能让一个数据库变得守得住。"
}
];
